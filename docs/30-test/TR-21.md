# TR-21 FR-06 이미지 분류 (rule-based)

실행: 2026-06-28
명령: `pytest tests/test_classifier.py -v` / `pytest --cov=src/pptx_md --cov-report=term-missing` / `ruff check . && black --check .` / `mypy src/`
브랜치: feature/15-16-parser
환경: Python 3.11.0, Windows 11

---

## 테스트 케이스 매트릭스

| TC | 대응 AC | 시나리오 | 구현 테스트명 | 결과 |
|----|---------|---------|--------------|------|
| TC-21-01 | AC1 | 정상: 디코딩 가능 이미지 → non-None ImageClass | test_ac1_분류_후_non_none | PASS |
| TC-21-02 | AC1 | 경계: enrich_images 호출 전 classification=None | test_ac1_분류_전_classification_none | PASS |
| TC-21-03 | AC1 | 예외: 빈 bytes → None (ImageClass 아님) | test_ac7_빈_bytes_none_반환 | PASS |
| TC-21-04 | AC2 | 정상: 고색상 그라데이션 사진 PNG → PHOTO | test_ac2_photo_분류 | PASS |
| TC-21-05 | AC2 | 결정성: PHOTO 동일 입력 3회 → 동일 출력 | QA 독립 검증 (python -c) | PASS |
| TC-21-06 | AC2 | 경계: 없음 (AC2는 PHOTO 분류 1종 케이스) | — | N/A |
| TC-21-07 | AC3 | 정상: 작은 RGBA + 단일색 → LOGO | test_ac3_logo_분류 | PASS |
| TC-21-08 | AC3 | 경계: alpha 없는 32×32 단색 불투명 → LOGO | test_ac3_logo_알파없이_단순_작은_이미지 | PASS |
| TC-21-09 | AC3 | 결정성: LOGO 동일 입력 3회 → 동일 출력 | QA 독립 검증 | PASS |
| TC-21-10 | AC4 | 정상: 격자+막대 PNG → DIAGRAM | test_ac4_diagram_분류 | PASS |
| TC-21-11 | AC4 | 결정성: DIAGRAM 동일 입력 3회 → 동일 출력 | QA 독립 검증 | PASS |
| TC-21-12 | AC4 | 경계: DIAGRAM 임계값 근방 (grid spacing 다름) | (임계값 근방 픽스처 없음 — NEXT 참조) | SKIP |
| TC-21-13 | AC5 | 정상: 흰배경+가로줄 텍스트 PNG → TEXT | test_ac5_text_분류 | PASS |
| TC-21-14 | AC5 | 결정성: TEXT 동일 입력 3회 → 동일 출력 | QA 독립 검증 | PASS |
| TC-21-15 | AC6 | 정상: enrich_images → classification 채워짐, 기타 필드 보존 | test_ac6_enrich_images_in_place_갱신 | PASS |
| TC-21-16 | AC6 | 정상: enrich_images 반환 None (in-place 의미론) | test_ac6_enrich_images_반환_none | PASS |
| TC-21-17 | AC6 | 경계: IR 스키마 필드명 집합 변경 없음 | test_ac6_ir_스키마_변경_없음 | PASS |
| TC-21-18 | AC7 | 정상: 디코딩 불가 bytes → None, 예외 없음 | test_ac7_디코딩_불가_bytes_none_반환 | PASS |
| TC-21-19 | AC7 | 경계: 빈 bytes → None, 예외 없음 | test_ac7_빈_bytes_none_반환 | PASS |
| TC-21-20 | AC7 | 예외: 잘린 PNG 헤더 → None, 예외 없음 | test_ac7_truncated_png_none_반환 | PASS |
| TC-21-21 | AC7 | 예외: 쓰레기 bytes 다종 → raise 없음 | test_ac7_예외_전파_없음 | PASS |
| TC-21-22 | AC8 | 정상: classifier.py에 anthropic/openai 0건 (grep) | test_ac8_vlm_import_없음_classifier | PASS |
| TC-21-23 | AC8 | 정상: image_pipeline.py에 anthropic/openai 0건 | test_ac8_vlm_import_없음_image_pipeline | PASS |
| TC-21-24 | AC8 | 정상: vector.py에 anthropic/openai 0건 | test_ac8_vlm_import_없음_vector | PASS |
| TC-21-25 | AC8 | 정상: LibreOffice 미설치 환경 import 성공 | test_ac8_import_no_importerror + QA 독립 검증 | PASS |
| TC-21-26 | AC9 | 정상: 동일 입력 2회 → 동일 출력 (4종 fixture) | test_ac9_결정성_photo/logo/diagram/text | PASS |
| TC-21-27 | AC9 | 경계: corrupt bytes 2회 → 동일 None | test_ac9_결정성_none_입력 | PASS |
| TC-21-28 | AC9 | 예외: mypy strict exit 0 | mypy src/ → "Success: no issues found" | PASS |

---

## 실행 로그 (요약)

### pytest tests/test_classifier.py -v
```
collected 34 items
34 passed, 25 warnings in 12.91s
```

### pytest --cov=src/pptx_md --cov-report=term-missing (전체 스위트)
```
collected 101 items — 101 passed, 25 warnings in 17.93s

Name                            Stmts   Miss  Cover   Missing
-------------------------------------------------------------
src\pptx_md\classifier.py         109      7    94%   116-117, 121, 128, 233, 262-263
src\pptx_md\image_pipeline.py      27      3    89%   48, 54-56
src\pptx_md\vector.py              51     25    51%   48, 80-81, 89-141
```

### ruff check . && black --check .
```
All checks passed!
All done! 13 files would be left unchanged.
```

### mypy src/
```
Success: no issues found in 7 source files
```

### QA 독립 검증 — anthropic/openai grep
```
grep "anthropic|openai" src/pptx_md/*.py → 0 matches
```

### QA 독립 검증 — LibreOffice 미설치 import
```
import pptx_md.vector → OK
libreoffice_available() → False (shutil.which 기반, 예외 없음)
```

### QA 독립 검증 — 결정성 3회 연속 호출
```
PHOTO: photo x3 -> consistent=True
LOGO:  logo  x3 -> consistent=True
DIAGRAM: diagram x3 -> consistent=True
TEXT: text x3 -> consistent=True
```

---

## 커버리지 미커버 라인 분석

| 모듈 | 미커버 라인 | 내용 | 판정 |
|------|------------|------|------|
| classifier.py | 116-117 | `img_thumb.convert("RGBA")` Exception 경로 | Pillow가 정상 포맷에서 convert 실패를 유발하기 어려움 — 방어 코드, 75% 초과이므로 NFR-02 충족 |
| classifier.py | 121 | `width==0 or height==0` 경로 | 0×0 이미지 생성 경로 없음 — 방어 코드 |
| classifier.py | 128 | `total==0` 경로 | getdata() 빈 결과 경로 없음 — 방어 코드 |
| classifier.py | 233 | `_classify_features` 마지막 `return None` | 4종 규칙 미해당 케이스 픽스처 부재 |
| classifier.py | 262-263 | `classify_image` debug log — result가 None인 경로 | 위 233번과 동일 케이스에서 발동 |
| image_pipeline.py | 48 | `not isinstance(slide, SlideIR)` — SlideIR 아닌 슬롯 | 현재 PresentationIR.slides는 SlideIR만 담음 — 방어 코드 |
| image_pipeline.py | 54-56 | `_enrich_single` 예상외 Exception 경로 | try/except 방어 블록 — 현실적 발동 없음 |
| vector.py | 89-141 | subprocess 실행 경로 전체 | LibreOffice 미설치 환경 — `@requires_libreoffice` skipif 설계 의도(ARCH-M3 §7.2) |

**신규 3모듈 커버리지**: classifier.py 94%, image_pipeline.py 89%, vector.py 51%
vector.py 미커버 구간은 모두 LibreOffice subprocess 실행 경로로, ARCH-M3 §7.2에서 `@requires_libreoffice` skipif로 격리하도록 설계된 의도된 미커버. classifier.py + image_pipeline.py 는 NFR-02(75%+) 충족. vector.py의 항상-실행 경로(VECTOR_EXTS, libreoffice_available, ext 거부, 미설치 graceful, import)는 모두 커버됨.

---

## 결함

없음. 전 TC 통과.

---

## 경고 (결함 아님)

- `Image.Image.getdata()` Deprecation Warning (Pillow 12 현재 동작 정상, 제거 예정 Pillow 14 / 2027-10-15). classifier.py:125, test_classifier.py:88, test_classifier.py:120 에서 발생. AC 요건과 무관, 기능 정상. NEXT로 기록.
