# TR-22 FR-07 LibreOffice 변환 (optional)

실행: 2026-06-28
브랜치: feature/22-libreoffice-converter
명령: `pytest tests/test_vector.py -v` / `pytest --cov=src/pptx_md --cov-report=term-missing`

---

## TC 매트릭스

| TC | 대응 AC | 시나리오 | 결과 |
|----|--------|---------|------|
| TC-22-01 | AC1 | LibreOffice 설치 시 EMF blob → PNG bytes 반환, Pillow 디코딩 가능 | SKIP (환경 미설치) |
| TC-22-02 | AC1 | LibreOffice 설치 시 WMF blob → 변환 시도 경로 진입 | SKIP (환경 미설치) |
| TC-22-03 | AC1 | fake-subprocess 로 subprocess+파일 실제 실행 경로 커버 | PASS |
| TC-22-04 | AC2 | LibreOffice 미설치 환경에서 `import pptx_md.vector` 성공 | PASS |
| TC-22-05 | AC2 | `libreoffice_available()` 항상 bool 반환, 예외 없음 | PASS |
| TC-22-06 | AC2 | `shutil.which` 경로 반환 시 `libreoffice_available()` → True | PASS |
| TC-22-07 | AC2 | `shutil.which` None 시 `libreoffice_available()` → False | PASS |
| TC-22-08 | AC2 | import 시점에 `shutil.which` 미호출 (import-time 부작용 0) | PASS |
| TC-22-09 | AC3 | `shutil.which` → None, EMF 입력 → `convert_vector_to_png` None 반환 | PASS |
| TC-22-10 | AC3 | `shutil.which` → None, WMF 입력 → None 반환 | PASS |
| TC-22-11 | AC3 | LibreOffice 부재 + 빈 bytes → 예외 없이 None 반환 | PASS |
| TC-22-12 | AC3 | LibreOffice 부재 + null bytes → 예외 없이 None 반환 | PASS |
| TC-22-13 | AC3 (배치연속성) | EMF skip 후 동일 배치 내 PNG shape 분류 계속 (독립 시나리오) | PASS |
| TC-22-14 | AC4 | ext='png' → VECTOR_EXTS 외 → None 반환, LibreOffice 미호출 | PASS |
| TC-22-15 | AC4 | ext='jpeg' → None 반환 | PASS |
| TC-22-16 | AC4 | ext='jpg' → None 반환 | PASS |
| TC-22-17 | AC4 | ext='bmp' → None 반환 | PASS |
| TC-22-18 | AC4 | 비벡터 ext 입력 시 `shutil.which` 자체 미호출 (spy 확인) | PASS |
| TC-22-19 | AC4 | VECTOR_EXTS에 'emf', 'wmf' 포함 확인 | PASS |
| TC-22-20 | AC4 | 대문자 'EMF' 입력 → `.lower()` 처리, 예외 없음 | PASS |
| TC-22-21 | AC5 | subprocess returncode≠0 → None + WARNING 로그 | PASS |
| TC-22-22 | AC5 | subprocess returncode≠0 → 예외 전파 없음 | PASS |
| TC-22-23 | AC5 | returncode=0 + 출력 파일 없음 → None + WARNING 로그 | PASS |
| TC-22-24 | AC5 | `subprocess.TimeoutExpired` → None + 예외 전파 없음 + WARNING | PASS |
| TC-22-25 | AC5 | 예상치 못한 OSError → None + 예외 전파 없음 + WARNING | PASS |
| TC-22-26 | AC5 | subprocess 성공 + 출력 파일 존재 → PNG bytes 반환 | PASS |
| TC-22-27 | AC5 | subprocess 성공 + 빈 출력 파일 → None 반환 | PASS |
| TC-22-28 | AC6 | vector.py 소스에 'anthropic'/'openai' 문자열 0건 | PASS |
| TC-22-29 | AC6 | vector.py 최상단 imports stdlib only (AST 파싱 검증) | PASS |
| TC-22-30 | AC6 | `mypy src/` exit 0 | PASS |
| TC-22-31 | AC6 | `grep -rE "anthropic|openai" src/pptx_md/vector.py` → 0건 (QA 독립 실행) | PASS |

---

## AC 커버리지 판정

| AC | 판정 | TC | 비고 |
|----|------|-----|------|
| AC1 변환 성공 | SKIP / PARTIAL PASS | TC-22-01, TC-22-02, TC-22-03 | 실환경 LibreOffice 미설치로 실변환 경로 SKIP. fake-subprocess 로 vector.py:119-128 라인 커버. 이슈 §비고 의 "CI LibreOffice 미설치 시 AC1은 로컬/문서 검증 보완" 조건 충족 |
| AC2 런타임 감지 | PASS | TC-22-04~08 | import 성공, bool 반환, which 분기, import-time 부작용 0 전항목 독립 확인 |
| AC3 LibreOffice 부재→skip | PASS | TC-22-09~13 | 단일 호출 skip + 배치 연속성 (TC-22-13 독립 시나리오) 모두 확인 |
| AC4 대상 형식 한정 | PASS | TC-22-14~20 | 비벡터 ext → early return, which 미호출, 대문자 ext 정규화 확인 |
| AC5 변환 실패 격리 | PASS | TC-22-21~27 | returncode≠0 / 출력없음 / Timeout / OSError 각 경로 독립 확인. WARNING 로그 caplog 어서션 |
| AC6 VLM 미의존 + mypy | PASS | TC-22-28~31 | grep 0건 QA 독립 실행, AST stdlib-only, mypy exit 0 |

---

## 실행 로그 (증거)

### test_vector.py 단독 실행
```
platform win32 -- Python 3.11.0, pytest-9.1.1
collected 28 items

tests/test_vector.py::test_ac1_변환_성공_png_bytes_반환 SKIPPED (LibreOffice not installed)
tests/test_vector.py::test_ac1_wmf_변환_시도 SKIPPED (LibreOffice not installed)
tests/test_vector.py::test_ac2_import_항상_성공 PASSED
tests/test_vector.py::test_ac2_libreoffice_available_bool_반환 PASSED
tests/test_vector.py::test_ac2_libreoffice_available_which_found PASSED
tests/test_vector.py::test_ac2_libreoffice_available_which_not_found PASSED
tests/test_vector.py::test_ac2_module_level_no_side_effects PASSED
tests/test_vector.py::test_ac3_which_none_emf_returns_none PASSED
tests/test_vector.py::test_ac3_which_none_wmf_returns_none PASSED
tests/test_vector.py::test_ac3_no_exception_on_empty_bytes PASSED
tests/test_vector.py::test_ac3_no_exception_on_null_bytes PASSED
tests/test_vector.py::test_ac4_png_ext_returns_none PASSED
tests/test_vector.py::test_ac4_jpeg_ext_returns_none PASSED
tests/test_vector.py::test_ac4_jpg_ext_returns_none PASSED
tests/test_vector.py::test_ac4_bmp_ext_returns_none PASSED
tests/test_vector.py::test_ac4_libreoffice_not_called_for_non_vector PASSED
tests/test_vector.py::test_ac4_vector_exts_contains_emf_wmf PASSED
tests/test_vector.py::test_ac4_uppercase_ext_treated_as_non_vector PASSED
tests/test_vector.py::test_ac5_subprocess_nonzero_returncode PASSED
tests/test_vector.py::test_ac5_subprocess_nonzero_no_exception PASSED
tests/test_vector.py::test_ac5_output_file_missing PASSED
tests/test_vector.py::test_ac5_timeout_returns_none PASSED
tests/test_vector.py::test_ac5_generic_exception_returns_none PASSED
tests/test_vector.py::test_ac5_successful_conversion_returns_png PASSED
tests/test_vector.py::test_ac5_empty_output_file_returns_none PASSED
tests/test_vector.py::test_ac6_no_vlm_imports_in_vector_source PASSED
tests/test_vector.py::test_ac6_vector_module_stdlib_only PASSED
tests/test_vector.py::test_ac6_mypy_exit_0 PASSED

vector.py: 51 stmts, 0 miss → 100%
======================== 26 passed, 2 skipped in 1.42s ========================
```

### 전체 스위트 실행 (`pytest --cov=src/pptx_md --cov-report=term-missing`)
```
121 passed, 2 skipped, 25 warnings in 7.57s

Name                            Stmts   Miss  Cover   Missing
-------------------------------------------------------------
src\pptx_md\__init__.py             1      0   100%
src\pptx_md\classifier.py         109      7    94%
src\pptx_md\errors.py               2      0   100%
src\pptx_md\image_pipeline.py      27      3    89%
src\pptx_md\ir.py                  66      0   100%
src\pptx_md\parser.py             159     27    83%
src\pptx_md\vector.py              51      0   100%
-------------------------------------------------------------
TOTAL                             415     37    91%
```

### lint / type 게이트
```
ruff check .        → All checks passed (exit 0)
black --check .     → 14 files unchanged (exit 0)
mypy src/           → Success: no issues found in 7 source files (exit 0)
```

### grep 게이트 (QA 독립 실행)
```
grep -rE "anthropic|openai" src/pptx_md/vector.py → 0건 (exit 1)
```

### TC-22-13 독립 배치 연속성 시나리오 실행 결과
```
EMF shape.classification: None        (LibreOffice 부재 → skip, None 유지)
PNG shape.classification: logo        (배치 내 다음 shape 정상 분류 계속)
TC-22-08 배치연속성 PASS: True
```

---

## 형식적 테스트 점검 (developer 단위 테스트)

| 항목 | 판정 | 근거 |
|------|------|------|
| TC-22-01/02 (AC1 skipif) | 정상 | `@_requires_libreoffice` 마커로 환경 격리. Then 절(PNG 시그니처 + Pillow 디코딩)을 직접 assert. `if result is not None:` 조건부 assert는 LibreOffice 변환 성공 보장 불가 EMF 스텁 특성상 허용 범위 |
| TC-22-08 (AC2 import-time) | 정상 | `ast.walk` 사용이 주석("함수 내부 제외")과 달리 모든 노드를 순회하나, vector.py 에 함수 내부 import가 없으므로 결과적으로 정확. 주석과 구현 불일치는 형식적 결함이 아닌 주석 오류 (NEXT 등록) |
| AC3 배치 연속성 | 주의 | test_vector.py 는 단일 함수 호출 수준만 검증. "배치 내 다른 이미지 처리 계속"은 image_pipeline.py 레벨 책임 — test_classifier.py::test_integration_enrich_emf_no_libreoffice 가 커버하며 QA 독립 실행(TC-22-13)으로 추가 확인 |
| AC5 WARNING 로그 | 정상 | caplog으로 `r.levelno >= logging.WARNING` assert. 원본 바이트 미출력 여부는 vector.py:113-116 소스 직접 확인(메타만 출력) |

---

## 결함 목록

없음.

---

## 비차단 관찰 (NEXT 등록 권장)

1. **test_ac6_vector_module_stdlib_only 주석 불일치**: 주석 "함수/클래스 내부 제외"와 달리 `ast.walk`는 전체 트리를 순회. 현재 vector.py에 함수 내부 import가 없으므로 결과 정확하나, 향후 함수 내부에 조건부 import가 추가되면 오탐 발생 가능. 최상단 노드만 검사하는 방식으로 수정 권장.
2. **AC1 실변환 경로 CI 미커버**: 본 환경 및 CI(LibreOffice 미설치) 에서 실제 soffice 프로세스 실행 경로 미검증. M6 단계에서 LibreOffice 설치 CI job 추가 권장 (이슈 §비고와 동일).
3. **Pillow `getdata` DeprecationWarning**: classifier.py:125 (`Image.getdata` → Pillow 14 제거 예정). FR-07 범위 밖, 기존 코드.
