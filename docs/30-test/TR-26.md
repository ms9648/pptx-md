# TR-26 — ImageDescriber 프로토콜 (FR-08 #26)

실행: 2026-06-28
브랜치: `feature/26-image-describer-protocol`
명령:
- `pytest tests/test_describer.py -v --tb=short`
- `pytest --cov=src/pptx_md --cov-report=term-missing`
- `ruff check . && black --check . && mypy src/`

---

## 테스트 케이스 매트릭스

| TC | 대응 AC | 시나리오 | 결과 |
|----|---------|---------|------|
| TC-26-01 | AC1 | `import pptx_md.describer` 성공, `None`이 아님 | PASS |
| TC-26-02 | AC1 | `pptx_md.describer.ImageDescriber` 심볼 접근 | PASS |
| TC-26-03 | AC1 | `__all__`에 `"ImageDescriber"` 포함 | PASS |
| TC-26-04 | AC1 | `DescribeError`, `InstallationError` import 및 계층 확인 | PASS |
| TC-26-05 | AC2 | `describe` 파라미터 3개(image_bytes, image_ext, shape_hint) 존재 | PASS |
| TC-26-06 | AC2 | `get_type_hints`로 타입 어노테이션 정확히 일치 (`bytes`, `str`, `str\|None` → `str`) | PASS |
| TC-26-07 | AC2 | 파라미터 순서 `[image_bytes, image_ext, shape_hint]` | PASS |
| TC-26-08 | AC3 | 상속 없는 클래스(`_FakeDescriber`) → `isinstance(obj, ImageDescriber)` True | PASS |
| TC-26-09 | AC3 | `describe` 없는 클래스 → `isinstance` False (경계: 프로토콜 불만족) | PASS |
| TC-26-10 | AC3 | `shape_hint=None` 전달 시 예외 없이 str 반환 | PASS |
| TC-26-11 | AC4 | GOOD 시그니처 클래스 → mypy --strict exit 0 | PASS |
| TC-26-12 | AC4 | BAD 시그니처 (return `int`) → mypy --strict exit 1 + error 메시지 포함 | PASS |
| TC-26-13 | AC5 | `describer.py` 소스에 `anthropic` 문자열 0건 | PASS |
| TC-26-14 | AC5 | `describer.py` 소스에 `openai` 문자열 0건 | PASS |
| TC-26-15 | AC5 | `describer.py` import 행에 VLM SDK 패턴 0건 | PASS |
| TC-26-16 | AC6 | `mypy src/` exit 0 ("no issues found in 8 source files") | PASS |
| TC-26-17 | AC6 | `ruff check .` exit 0 ("All checks passed!") | PASS |
| TC-26-18 | AC6 | `black --check .` exit 0 ("16 files would be left unchanged") | PASS |

**전체: 18 passed, 0 failed, 0 skipped**

---

## AC 판정표

| AC | 내용 | 판정 | 근거 TC |
|----|------|------|---------|
| AC1 | `import pptx_md.describer` 성공 + `ImageDescriber` 심볼 노출 | PASS | TC-26-01~04 |
| AC2 | `describe(self, image_bytes: bytes, image_ext: str, shape_hint: str\|None) -> str` 시그니처 일치 | PASS | TC-26-05~07 |
| AC3 | 상속 없이 `describe` 구현한 클래스 → `isinstance(obj, ImageDescriber)` True | PASS | TC-26-08~10 |
| AC4 | 불일치 시그니처 → mypy exit 1 / GOOD 시그니처 → exit 0 | PASS | TC-26-11~12 |
| AC5 | `describer.py` 소스 anthropic/openai grep 0건 | PASS | TC-26-13~15 |
| AC6 | mypy/ruff/black 모두 exit 0 | PASS | TC-26-16~18 |

---

## 커버리지

```
Name                            Stmts   Miss  Cover   Missing
-------------------------------------------------------------
src\pptx_md\__init__.py             1      0   100%
src\pptx_md\classifier.py         109      7    94%   116-117, 121, 128, 233, 262-263
src\pptx_md\describer.py            6      0   100%
src\pptx_md\errors.py               4      0   100%
src\pptx_md\image_pipeline.py      27      3    89%   48, 54-56
src\pptx_md\ir.py                  66      0   100%
src\pptx_md\parser.py             159     27    83%   58-59, 94-101, 218-221, 228, 247-249, 284, 289-294, 317, 325-327, 338, 341, 343-344
src\pptx_md\vector.py              51      0   100%
-------------------------------------------------------------
TOTAL                             423     37    91%
```

`describer.py` 커버리지: **100%** (6 stmts, 0 miss)

---

## Lint / Type 검사 실행 로그

```
$ python -m ruff check .
All checks passed!
RUFF exit: 0

$ python -m black --check .
All done! ✨ 🍰 ✨
16 files would be left unchanged.
BLACK exit: 0

$ python -m mypy src/
Success: no issues found in 8 source files
MYPY exit: 0
```

---

## AC5 grep 결과

```
$ grep -rE "anthropic|openai" src/pptx_md/describer.py
(0건 — 출력 없음)
```

---

## 형식적 테스트 점검

developer 단위 테스트 18개 전수 점검 결과:

- AC1 테스트: `importlib.import_module` + `__all__` 직접 검증. 실질적.
- AC2 테스트: `inspect.signature` + `typing.get_type_hints` 조합으로 타입/순서 검증. 실질적.
- AC3 테스트: `isinstance` True/False 쌍 + `shape_hint=None` 경계 케이스 포함. 실질적.
- AC4 테스트: 실제 mypy subprocess 호출, exit code 검증. GOOD/BAD 양쪽 구체적 케이스. 실질적.
- AC5 테스트: 소스 파일 텍스트 직접 검사. 실질적.
- AC6 테스트: ruff/black/mypy subprocess 실제 실행, exit code 검증. 실질적.

형식적 테스트(AC Then 절 미검증) 없음.

---

## 결함 목록

없음.

---

## NEXT (개선 제안, AC 범위 외)

- `parser.py` 83% 커버리지: 미커버 라인 다수(58-59, 94-101 등) — 향후 M5/M6 테스트 보강 권장
- Pillow `Image.getdata()` DeprecationWarning (Pillow 14에서 제거 예정): M6 이전 `get_flattened_data` 교체 권장
