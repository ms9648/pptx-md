# TR-27 FR-09 VLM 제공자 구현

실행: 2026-06-28
명령: `pytest tests/test_providers.py -v` / `pytest --cov=src/pptx_md --cov-report=term-missing` / `ruff check . && black --check .` / `mypy src/`
브랜치: feature/27-vlm-providers

| TC | 대응 AC | 시나리오 | 결과 |
|----|---------|---------|------|
| TC-27-01 | AC1 | SDK None 주입 상태에서 `import pptx_md.providers` 성공 | PASS |
| TC-27-02 | AC1 | SDK None 주입 상태에서 `import pptx_md.providers.anthropic` 성공 | PASS |
| TC-27-03 | AC1 | SDK None 주입 상태에서 `import pptx_md.providers.openai` 성공 | PASS |
| TC-27-04 | AC1 | 최상단 SDK import 0건 (grep 검증) | PASS |
| TC-27-05 | AC2 | anthropic SDK None → `AnthropicDescriber()` → `InstallationError` + pip 안내 | PASS |
| TC-27-06 | AC2 | openai SDK None → `OpenAIDescriber()` → `InstallationError` + pip 안내 | PASS |
| TC-27-07 | AC2 | raw `ModuleNotFoundError` 미노출 (Anthropic) | PASS |
| TC-27-08 | AC2 | `get_describer('gemini')` → `ValueError` (미지원 provider) | PASS |
| TC-27-09 | AC3 | mock Anthropic SDK → `describe()` 텍스트 반환 | PASS |
| TC-27-10 | AC3 | mock Anthropic SDK → `shape_hint='flow_diagram'` 프롬프트 포함 | PASS |
| TC-27-11 | AC3 | mock OpenAI SDK → `describe()` 텍스트 반환 | PASS |
| TC-27-12 | AC3 | mock OpenAI SDK → `shape_hint='chart'` 프롬프트 포함 | PASS |
| TC-27-13 | AC3 | `shape_hint=None` → 프롬프트에 literal "None" 미포함, 정상 반환 | PASS |
| TC-27-14 | AC4 | `isinstance(AnthropicDescriber(...), ImageDescriber)` == True | PASS |
| TC-27-15 | AC4 | `isinstance(OpenAIDescriber(...), ImageDescriber)` == True | PASS |
| TC-27-16 | AC4 | `describe` 없는 클래스 → `isinstance` == False (음성 케이스) | PASS |
| TC-27-17 | AC5 | mock Anthropic SDK 예외 → `DescribeError` 전파 | PASS |
| TC-27-18 | AC5 | `DescribeError` 메시지에 API key 미포함 (Anthropic) | PASS |
| TC-27-19 | AC5 | `DescribeError` 메시지에 `image_bytes` 미포함 (Anthropic) | PASS |
| TC-27-20 | AC5 | mock OpenAI SDK 예외 → `DescribeError` 전파 | PASS |
| TC-27-21 | AC5 | `DescribeError` 메시지에 API key 미포함 (OpenAI) | PASS |
| TC-27-22 | AC6 | `describe()` 실행 중 로그에 API key 미출력 (Anthropic, caplog) | PASS |
| TC-27-23 | AC6 | `describe()` 실행 중 로그에 `image_bytes` 미출력 (Anthropic) | PASS |
| TC-27-24 | AC6 | `describe()` 실행 중 로그에 API key 미출력 (OpenAI) | PASS |
| TC-27-25 | AC6 | `describe()` 실행 중 로그에 `image_bytes` 미출력 (OpenAI) | PASS |
| TC-27-26 | AC7 | `pytest` 165 passed, 2 skipped — 네트워크 호출 0건 (전부 mock) | PASS |
| TC-27-27 | AC7 | `mypy src/` exit 0 (11 source files) | PASS |
| TC-27-28 | AC7 | `ruff check .` exit 0 | PASS |
| TC-27-29 | AC7 | `black --check .` exit 0 (20 files unchanged) | PASS |

## 실행 로그

### pytest tests/test_providers.py -v

```
platform win32 -- Python 3.11.0, pytest-9.1.1
26 passed in 11.12s

Name                                  Stmts  Miss  Cover
---------------------------------------------------------
src/pptx_md/providers/__init__.py        20    11    45%
src/pptx_md/providers/anthropic.py       45     2    96%
src/pptx_md/providers/openai.py          49     2    96%
```

### pytest --cov=src/pptx_md --cov-report=term-missing (전체)

```
165 passed, 2 skipped, 25 warnings in 37.88s

Name                                 Stmts   Miss  Cover
----------------------------------------------------------
src/pptx_md/providers/__init__.py       20     11    45%
src/pptx_md/providers/anthropic.py      45      2    96%
src/pptx_md/providers/openai.py         49      2    96%
TOTAL                                  537     52    90%
```

### lint / type

```
ruff check .     → All checks passed!  (exit 0)
black --check .  → 20 files would be left unchanged  (exit 0)
mypy src/        → Success: no issues found in 11 source files  (exit 0)
```

### 최상단 SDK import 0건 확인

```
grep -rE "^import anthropic|^import openai|^from anthropic|^from openai" \
  src/pptx_md/providers/anthropic.py \
  src/pptx_md/providers/openai.py \
  src/pptx_md/providers/__init__.py
→ 매치 0건 (exit 1 = no match)
```

## 커버리지 비고

- `providers/__init__.py` 45%: `get_describer` 정상 경로(`__init__.py:58-64`)와 `__getattr__` 재노출 경로(`72-81`)가 미커버. 어느 AC도 이 경로를 요구하지 않으며, 핵심 구현 파일(anthropic.py 96%, openai.py 96%)은 NFR-02(75%) 충족.
- `providers/anthropic.py` miss 2건: L36(`alt_text` 있는 경우 분기), L48(empty content 분기) — AC 범위 외 경계, 차단 사유 아님.
- `providers/openai.py` miss 2건: 동형 (L36, L51).

## 독립 검증 관찰 사항 (비차단)

1. **`__init__.py` lazy `__getattr__` 재노출 미테스트**: `from pptx_md.providers import AnthropicDescriber` 경로가 단위 테스트에서 검증되지 않음(직접 모듈 import 사용). AC 요구사항이 아니므로 비차단 — M6 보강 권고.
2. **`alt_text` 파라미터 미사용**: `_build_prompt(shape_hint, alt_text)` 시그니처가 `alt_text`를 받지만 `describe()` 호출 시 항상 `""` 전달. FR-10(#28) 연동 시 확장 지점으로 식별됨 — 현재 범위 외.
