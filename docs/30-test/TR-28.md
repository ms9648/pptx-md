# TR-28 FR-10 shape_hint 오케스트레이터 QA 리포트

실행: 2026-06-28
브랜치: feature/28-shape-hint-orchestrator
명령:
- `pytest tests/test_description_pipeline.py -v` (developer 테스트 독립 실행 확인)
- `pytest --cov=src/pptx_md --cov-report=term-missing` (전체 스위트 + 커버리지)
- `python -m ruff check . && python -m black --check .` (lint)
- `python -m mypy src/` (타입 검사)
- `python qa_tc28.py` (QA 독립 시나리오 37건)

## 테스트 매트릭스

| TC | 대응 AC | 시나리오 | 유형 | 결과 |
|----|---------|---------|------|------|
| TC-28-01 | AC1 | DIAGRAM 이미지 1개 + FakeDescriber → description in-place 채워짐, 반환 None | 정상 | PASS |
| TC-28-02 | AC1 | 빈 presentation (슬라이드 0개) → describe 0회, 반환 None | 경계 | PASS |
| TC-28-03 | AC1 | 이미지 없는 슬라이드 (텍스트만) → describe 0회, 반환 None | 경계 | PASS |
| TC-28-04 | AC2 | ImageClass.DIAGRAM → shape_hint에 "diagram" 포함 | 정상 | PASS |
| TC-28-05 | AC2 | classification=None → shape_hint=None 전달 | 경계 | PASS |
| TC-28-06 | AC2 | _shape_hint() 4종 enum + None 직접 단언 (TEXT/DIAGRAM/PHOTO/LOGO) | 예외 | PASS |
| TC-28-07 | AC3 | describer=None, 이미지 3개 → 전부 description=None, 예외 0건 | 정상 | PASS |
| TC-28-08 | AC3 | describer=None + 빈 presentation → None 반환, 예외 없음 | 경계 | PASS |
| TC-28-09 | AC4 | 첫 이미지 describe() 예외 → 해당 None+WARNING(shape_id 포함), 후속 2건 정상 | 정상 | PASS |
| TC-28-10 | AC4 | 모든 이미지 예외 → 전부 None, 예외 미전파 | 경계 | PASS |
| TC-28-11 | AC5 | GroupShapeIR 1단 중첩 ImageShapeIR → iter_shapes 재귀 도달, description 채워짐 | 정상 | PASS |
| TC-28-12 | AC5 | GroupShapeIR 2단 중첩 ImageShapeIR → 재귀 도달 | 경계 | PASS |
| TC-28-13 | AC5 | GroupShapeIR + 평탄 ImageShapeIR 혼재 → 둘 다 채워짐, describe 2회 | 예외 | PASS |
| TC-28-14 | AC6 | image_bytes 센티넬 값 → 로그 전체(DEBUG 포함) 미출력, shape_id는 출력 | 정상 | PASS |
| TC-28-15 | AC6 | description 전문 센티넬 값 → 로그 미출력 | 경계 | PASS |
| TC-28-16 | AC6 | describe() 예외 시 WARNING 메타 구성 확인 (shape_id 포함, 예외 전문 미포함) | 예외 | PASS |
| TC-28-17 | AC7 | description_pipeline.py 소스 텍스트 직접 검사: anthropic/openai 0건 | 정상 | PASS |

전체: 17 TC × 37 단언 — PASS 37, FAIL 0

## 실행 로그

### developer 테스트 (34건)

```
platform win32 -- Python 3.11.0, pytest-9.1.1
tests/test_description_pipeline.py — 34 passed in 7.16s
description_pipeline.py 커버리지: 98% (42 stmts, miss 1 = line 106 debug-only)
```

### 전체 스위트

```
199 passed, 2 skipped (LibreOffice 미설치), 0 failed — 43.89s
description_pipeline.py: 98% (line 106 miss — describer=None debug log, 무해)
```

### lint / 타입 검사

```
ruff check .: All checks passed!
black --check .: 22 files would be left unchanged.
mypy src/: Success: no issues found in 12 source files
```

### grep SDK import

```
grep -rE "anthropic|openai" src/pptx_md/description_pipeline.py
(0건 — exit code 1)
```

### QA 독립 시나리오

```
PASS: 37  FAIL: 0
ALL TC PASSED
```

## developer 테스트 충실성 점검

| 항목 | 판정 | 근거 |
|------|------|------|
| AC1 단언이 Then 절 직접 검증 | PASS | `assert shape.description == "a nice diagram"`, `assert result is None` — 값 직접 비교 |
| AC2 단언이 Then 절 직접 검증 | PASS | `assert describer.calls[0]["shape_hint"] is None`, `assert "diagram" in hint.lower()` |
| AC3 예외 0건 단언 | PASS | `pytest.fail(...)` 가드로 예외 발생 여부 검증 |
| AC4 격리 단언 (shape1=None, shape2 채워짐) | PASS | 두 shape 각각 직접 단언 |
| AC5 중첩 도달 단언 | PASS | `assert inner_image.description == "logo desc"` 직접 단언 |
| AC6 bytes 미출력 단언 | PASS | `caplog.text`에 센티넬 문자열 부재 단언 |
| AC7 subprocess 직접 실행 후 returncode 단언 | PASS | 실 도구 실행 결과 검증 |
| 항상-참(trivial) 단언 존재 여부 | 없음 | FakeDescriber 호출 기록으로 실 호출 경로 검증 |

## AC 커버리지 요약

| AC | 커버 TC | 판정 |
|----|---------|------|
| AC1 (description in-place, 반환 None) | TC-28-01,02,03 | PASS |
| AC2 (DIAGRAM→hint, None→None) | TC-28-04,05,06 | PASS |
| AC3 (describer=None → 전부 None, 예외 0) | TC-28-07,08 | PASS |
| AC4 (첫 이미지 예외 → 격리+WARNING, 후속 정상) | TC-28-09,10 | PASS |
| AC5 (GroupShapeIR 중첩 → iter_shapes 재귀) | TC-28-11,12,13 | PASS |
| AC6 (로그 메타만, bytes/텍스트 전문 미출력) | TC-28-14,15,16 | PASS |
| AC7 (SDK grep 0건 + mypy/ruff/black exit 0) | TC-28-17 + lint 명령 | PASS |

AC 커버리지: 7/7 (100%)

## 비고

- line 106 (`describer=None` 분기의 debug 로그) 미커버: `describer=None` 시 즉시 return 처리로 debug 로그가 도달하지 않는 경로. AC3 충족에 영향 없음.
- `_shape_hint()` 에서 `_HINTS.get(classification)` 는 등록되지 않은 ImageClass 값에 대해 `None` 반환. 현재 4종 enum이 전부 등록되어 있어 실운용 상 도달 불가한 경로.
- Pillow `Image.getdata()` DeprecationWarning은 classifier.py 관련 (이슈 #28 범위 외), 미차단.
