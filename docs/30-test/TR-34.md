# TR-34 FR-12 Map-Reduce 파이프라인

실행: 2026-06-28
브랜치: feature/34-map-reduce-pipeline
명령:
- `pytest tests/test_pipeline.py -v` (developer 테스트 26개)
- `pytest --cov=src/pptx_md --cov-report=term-missing` (전체 스위트)
- `python qa_tc34.py` (QA 독립 시나리오 22개)
- `ruff check . && black --check . && mypy src/`

## 테스트 케이스 매트릭스

| TC | 대응 AC | 시나리오 | 결과 |
|----|---------|----------|------|
| TC-34-01 | AC1 | 두 슬라이드 블록 포함 단일 str 반환 | PASS |
| TC-34-02 | AC1 | 단일 슬라이드 str 반환 (경계) | PASS |
| TC-34-03 | AC2 | index=2,0,1 입력 → 0,1,2 순서 출력 | PASS |
| TC-34-04 | AC2 | 역순 5개 슬라이드 오름차순 정렬 (경계) | PASS |
| TC-34-05 | AC3 | 구분자 정확히 N-1개, 선두·말미 없음 | PASS |
| TC-34-06 | AC3 | 단일 슬라이드 구분자 0개 (경계) | PASS |
| TC-34-07 | AC3 | gap 문자열이 _SLIDE_SEPARATOR와 정확히 일치 | PASS |
| TC-34-08 | AC4 | description=None 이미지 3개 — 예외없음 | PASS |
| TC-34-09 | AC4 | assembler.py AST 레벨 VLM import 없음 (anthropic, openai) | PASS |
| TC-34-10 | AC4 | alt_text/classification/description 모두 None → _[image]_ | PASS |
| TC-34-11 | AC5 | 동일 IR 2회 호출 → 동일 문자열 | PASS |
| TC-34-12 | AC5 | 역순 입력에서도 결정성 유지 (경계) | PASS |
| TC-34-13 | AC5 | assembler 호출 후 IR 불변성 확인 | PASS |
| TC-34-14 | AC6 | slides=[] → 빈 문자열 "" | PASS |
| TC-34-15 | AC6 | 빈 결과에 구분자 없음 (경계) | PASS |
| TC-34-16 | AC6 | slides=[] 예외없이 str 반환 (예외 경로) | PASS |
| TC-34-17 | AC7 | OtherShapeIR 포함 슬라이드 → 전체 문서 생성 | PASS |
| TC-34-18 | AC7 | 빈 fallback_text OtherShape 생략, 후속 슬라이드 계속 (경계) | PASS |
| TC-34-19 | AC7 | 단일 슬라이드 내 OtherShape 5개 전부 포함 (예외) | PASS |
| TC-34-20 | AC8 | 20슬라이드 (텍스트+표+이미지+other) 0.0002s < 5s | PASS |
| TC-34-21 | AC8 | 50슬라이드 확장 — 예외없음, 0.0002s (경계) | PASS |
| TC-34-22 | AC8+AC4 | 20슬라이드 description=None 이미지 → placeholder 60개 정확 | PASS |

## developer 테스트 단언 충실성 점검

| AC | developer 테스트 | 단언 충실성 | 판정 |
|----|-----------------|-------------|------|
| AC1 | TestAc1 (3개) | `isinstance(doc, str)`, title/content `in doc` — Then 직접 검증 | PASS |
| AC2 | TestAc2 (3개) | `pos_zero < pos_one < pos_two` — 위치 비교로 순서 직접 검증 | PASS |
| AC3 | TestAc3 (5개) | `count(separator)==1`, startswith/endswith, gap 문자열 일치 | PASS |
| AC4 | TestAc4 (4개) | `isinstance(doc, str)` + `hasattr(mod, name)` 검사. hasattr 방식은 AST 검증보다 약하나 QA 독립 AST 검증으로 보완 확인 | PASS |
| AC5 | TestAc5 (3개) | `doc1==doc2`, IR 불변성 (`slide.title`, `para.text`) 직접 단언 | PASS |
| AC6 | TestAc6 (3개) | `result==""`, separator 없음 | PASS |
| AC7 | TestAc7 (4개) | 제목 `in doc` 확인. fallback_text 내용 `in doc` 포함 | PASS |
| AC8 | TestAc8 (1개) | `elapsed < 5.0` + 20개 슬라이드 제목 전수 확인 | PASS |

비차단 관찰: AC4의 `hasattr(mod, name)` 방식은 assembler가 이미 import된 상태에서 재로드 없이 기존 모듈 객체를 검사한다. `anthropic`/`openai`가 다른 모듈 경로로 간접 노출된 경우 탐지 못 할 수 있다. QA 독립 AST 검증(TC-34-09)으로 실증 보완 완료.

## 커버리지

```
전체 스위트 278 passed, 2 skipped / assembler.py 80% (122 stmts, 25 miss)
미커버 구간:
  L98-106  : assemble_document 슬라이드-레벨 예외 격리 폴백 — AC에 미포함
  L151-164 : _render_shape 알 수 없는 서브클래스 / 도형-레벨 예외 — AC에 미포함
  L174     : _render_text masking 적용 경로
  L191     : _render_table rows=[] 빈 표 경로
  L232,240 : _render_image description/alt_text+masking 조합
  L268     : _render_other fallback+masking
  L284-299 : _apply_masking 실제 실행 경로 (masking.enabled=True)
```

75% 기준(프로파일 §7) 충족: assembler.py 80%.

## 린트 / 타입 검사

```
ruff check .      : All checks passed (exit 0)
black --check .   : 27 files unchanged (exit 0)
mypy src/         : Success: no issues found in 14 source files (exit 0)
```

## 실행 로그 발췌

```
tests/test_pipeline.py: 26 passed in 0.56s
전체 스위트: 278 passed, 2 skipped in 50.26s
QA 독립 시나리오: 22 PASS / 0 FAIL
TC-34-20 (AC8): 20슬라이드 0.0002s < 5s
TC-34-21 (AC8): 50슬라이드 확장 0.0002s
```

## RESULT: DONE
