# TR-43 FR-16 공개 API (convert / ConvertOptions)

실행: 2026-06-28
브랜치: feature/43-public-api @ 34329ce
환경: Python 3.11.0 / pytest-9.1.1 / Windows 11

명령:
- `pytest --cov=src/pptx_md --cov-report=term-missing --cov-fail-under=75`
- `ruff check . && black --check . && mypy src/`

전체 결과: **363 passed, 2 skipped, 0 failed / coverage 92.15% (게이트 75% PASS)**

---

## 테스트 케이스 매트릭스

| TC | 대응 AC | 시나리오 | 검증 방식 | 결과 |
|----|---------|---------|-----------|------|
| TC-43-01 | AC1 | 유효한 PPTX → `convert(path)` → str, `##` 헤딩 포함 | QA 독립 e2e (실 PPTX 픽스처) | PASS |
| TC-43-02 | AC1 | `convert(str_path)` 수용 확인 | e2e str 경로 호출 | PASS |
| TC-43-03 | AC1 | 파이프라인 단계 순서 (parse→enrich_images→enrich_descriptions→assemble) | 호출 로그 단언 | PASS |
| TC-43-04 | AC2 | `pptx_md.__all__` = 정확히 12개 ADR-603 심볼 집합 | QA 독립 `sorted(pptx_md.__all__)` 직접 확인 | PASS |
| TC-43-05 | AC2 | 내부 모듈명(parse_presentation, enrich_images 등) 미포함 | `not in __all__` 확인 | PASS |
| TC-43-06 | AC2 | `__all__` 전 심볼 import 가능 | `hasattr(pptx_md, name)` | PASS |
| TC-43-07 | AC3 | core-only 환경 `import pptx_md` → ImportError 없음 | QA 독립 직접 실행 | PASS |
| TC-43-08 | AC3 | `anthropic`, `openai` sys.modules 미포함 | QA 독립 sys.modules 확인 | PASS |
| TC-43-09 | AC4 | fake describer 주입 → `describe()` 실 호출 + 반환 텍스트 최종 MD에 포함 (enrich_descriptions/assemble 실로직) | QA 독립 e2e `QA_UNIQUE_DESCRIPTION_TOKEN` 토큰 확인 | PASS |
| TC-43-10 | AC4 | describer 객체가 enrich_descriptions에 정확히 전달됨 | spy 패턴으로 전달 인자 캡처 | PASS |
| TC-43-11 | AC5 | `MaskingOptions(enabled=True)` → 이메일 → `[REDACTED]` (assemble 실로직) | QA 독립 e2e `qa_test@example.com` 미존재 + `[REDACTED]` 존재 확인 | PASS |
| TC-43-12 | AC5 | masking 옵션이 assemble_document에 kwarg로 전달됨 | 캡처 단언 | PASS |
| TC-43-13 | AC6 | 존재하지 않는 경로 → `ParseError` raise | QA 독립 직접 호출 확인 | PASS |
| TC-43-14 | AC6 | 비-PPTX 파일 → `ParseError` raise | QA 독립 직접 호출 확인 (`ParseError` 명시) | PASS |
| TC-43-15 | AC7 | 슬라이드 0개(빈) PPTX → 예외 없이 str 반환 | QA 독립 e2e | PASS |
| TC-43-16 | AC7 | 빈 PresentationIR → 예외 없이 str 반환 | monkeypatch | PASS |
| TC-43-17 | AC8 | `src/pptx_md/py.typed` 존재 + 파일 여부 | QA 독립 pathlib 확인 | PASS |
| TC-43-18 | AC8 | `mypy src/` → exit 0 | QA 독립 직접 실행: "no issues in 17 files" | PASS |
| TC-43-19 | AC9 | `validate=True` → `validate_markdown` 1회 호출 | QA 독립 호출 카운트 확인 | PASS |
| TC-43-20 | AC9 | `validate=False` (기본) → `validate_markdown` 미호출 | 카운트 = 0 확인 | PASS |
| TC-43-21 | AC9 | invalid 문서 + `validate=True` → WARNING 로그 발생 | QA 독립 log record 캡처 | PASS |
| TC-43-22 | AC9 | `validate=True/False` 모두 반환 타입 str 고정 (ADR-604) | isinstance 확인 | PASS |

---

## 게이트 체크

| 항목 | 결과 |
|------|------|
| pytest 전체 스위트 | 363 passed, 2 skipped, 0 failed |
| coverage (게이트 75%) | 92.15% PASS |
| ruff check | All checks passed |
| black --check | 33 files unchanged |
| mypy src/ | no issues found in 17 source files |

---

## AC 충족 요약

| AC | Then 조건 | QA 판정 | 근거 |
|----|-----------|---------|------|
| AC1 | `convert(path)` → str, VLM 없이 슬라이드 헤딩 포함 | PASS | TC-43-01 e2e: `'## Hello World Slide\n\nHello World Slide'` 반환 확인 |
| AC2 | `__all__` 12개, 내부 모듈명 미포함 | PASS | TC-43-04/05/06 QA 직접 확인: `sorted(__all__)` = 12개, ADR-603 집합 정확 일치 |
| AC3 | core-only → ImportError 없음 | PASS | TC-43-07/08 QA 직접 확인: anthropic/openai loaded=False |
| AC4 | fake describer 호출 + MD 반영 | PASS | TC-43-09 QA 독립 e2e: enrich_descriptions/assemble 실로직 통과, `QA_UNIQUE_DESCRIPTION_TOKEN` 반환 MD 포함 |
| AC5 | `MaskingOptions(enabled=True)` → `[REDACTED]` | PASS | TC-43-11 QA 독립 e2e: assemble_document 실로직, `qa_test@example.com` 미존재 + `[REDACTED]` 존재 |
| AC6 | 존재하지 않는 경로 → `ParseError` | PASS | TC-43-13/14 QA 직접 확인: `ParseError` 명시 raise |
| AC7 | 빈 PPTX → 예외 없이 str 반환 | PASS | TC-43-15 QA 독립 e2e: `''` 반환 (str) |
| AC8 | `py.typed` 존재 + `mypy src/` exit 0 | PASS | TC-43-17/18 QA 직접 확인 |
| AC9 | `validate=True` → `validate_markdown` + 경고 로깅 | PASS | TC-43-19/21 QA 독립 확인: called=1, WARNING 로그 발생 |

---

## 주목할 사항 (결함 없음)

**TC-43-14 관찰 (NEXT 이월, FAIL 아님)**
`test_ac6_invalid_file_raises_parse_error`는 `pytest.raises((ParseError, Exception))`으로 Exception을 광범위 허용한다.
그러나 QA 독립 검증(TC-43-14)에서 비-PPTX 파일에 대해 실제로 `ParseError`가 raise되는 것을 확인함 — AC6 Then("다른 예외 타입으로 새지 않는다")은 실제로 충족된다.
테스트 단언의 엄격성 문제는 NEXT 이월(이전 RE-REVIEW에서도 동일하게 기록됨).

---

## 실행 로그 증거 (전체 스위트)

```
tests\test_api.py .......................                                [  6%]
tests\test_assembler.py ............................                     [ 13%]
tests\test_classifier.py ............................                    [ 21%]
tests\test_describer.py ..................                               [ 26%]
tests\test_description_pipeline.py ..................................    [ 35%]
tests\test_ir.py ...........................                             [ 43%]
tests\test_masking.py ........................                           [ 49%]
tests\test_mermaid_fallback.py .........................                 [ 56%]
tests\test_parser.py .......................................             [ 67%]
tests\test_pipeline.py ..........................                        [ 74%]
tests\test_providers.py ..........................                       [ 81%]
tests\test_smoke.py .                                                    [ 81%]
tests\test_validator.py ......................................           [ 92%]
tests\test_vector.py ss..........................                        [100%]

Total coverage: 92.15% >= 75% PASS
363 passed, 2 skipped, 25 warnings in 26.41s
```
