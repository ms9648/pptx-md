# TR-45 FR-18 README + 사용 예시 + API 레퍼런스

실행: 2026-06-29 (재검증) / 브랜치: feature/45-docs (커밋: 1a8184d)
명령: `hatch build` / `pytest --cov=src/pptx_md --cov-report=term-missing` / `ruff check . && black --check .` / `mypy src/`

## 테스트 케이스 매트릭스

| TC | 대응 AC | 시나리오 | 결과 |
|----|---------|---------|------|
| TC-45-01 | AC1 | README.md 존재 + hatch build 경고 없이 성공 | PASS |
| TC-45-02 | AC1 | sdist PKG-INFO에 long_description(README 본문) 포함 여부 | PASS |
| TC-45-03 | AC2 | Quick Start 코드 블록 — 공개 API(from pptx_md import)만 사용 | PASS |
| TC-45-04 | AC2 | README 전체 코드 블록 — 내부 모듈 직접 호출 예시 존재 여부 | PASS |
| TC-45-05 | AC3 | pip install pptx-md (core) 설치 경로 기술 여부 | PASS |
| TC-45-06 | AC3 | pip install pptx-md[vlm] (VLM extras) 설치 경로 기술 여부 | PASS |
| TC-45-07 | AC4 | VLM 예시 코드 블록 내 평문 API 키 리터럴 없음 | PASS |
| TC-45-08 | AC4 | README에 os.environ 환경변수 참조 존재 | PASS |
| TC-45-09 | AC5 | MaskingOptions(enabled=True) 예시 포함 | PASS |
| TC-45-10 | AC5 | 커스텀 패턴(re.compile/patterns=) 예시 포함 | PASS |
| TC-45-11 | AC6 | docs/api.md 심볼 수 == pptx_md.__all__ (12개) 정합 | PASS |
| TC-45-12 | AC6 | __all__에 존재하나 docs/api.md 누락 심볼 0건 | PASS |
| TC-45-13 | AC6 | docs/api.md에 존재하나 __all__ 미등재 심볼 0건 | PASS |
| TC-45-14 | AC7 | README.md 내부 상대 링크 깨짐 0건 | PASS |
| TC-45-15 | AC7 | docs/usage.md 내부 상대 링크 깨짐 0건 | PASS |
| TC-45-16 | AC7 | docs/api.md 내부 상대 링크 깨짐 0건 | PASS |

## AC2 재검증 — 이전 BLOCKED 사유 해소 확인

### 이전 결함 (1차 TR)

README.md `## 내부 모듈 직접 접근` 섹션에 `from pptx_md.parser import parse_presentation` 코드 블록이 존재하였고, `_extract_code_blocks` 정규식 버그(`re.DOTALL` + greedy하지 않은 `.*?`)로 인해 마지막 블록이 추출되지 않아 미탐지되었음.

### 수정 내용 검증 (커밋 1a8184d)

1. README.md `## 내부 모듈 직접 접근 (SemVer 보증 밖)` 섹션 완전 제거 확인:
   - `parse_presentation in README: False` (독립 Python 스크립트 직접 확인)
   - 11개 내부 심볼 전수 검사 결과 모두 미발견

2. `_extract_code_blocks` splitlines 방식 교체 효과 검증:
   - 구버전 정규식: 10개 블록 추출 (마지막 블록 누락)
   - 신버전 splitlines: 11개 블록 추출 (마지막 블록 포함)
   - 11개 블록 전체 내부 심볼 검사: 위반 없음

### 경계 케이스 — _extract_code_blocks 마지막 블록 캡처 확인

block[10] (마지막 블록): `from pptx_md import convert, ParseError, DescribeError ...` — 공개 API만 사용, 내부 심볼 없음.

---

## 빌드/린트/타입 결과 (실행 로그)

### hatch build (AC1)

```
Inspecting build dependencies
------------------------------------ sdist ------------------------------------
dist\pptx_md-0.1.0.tar.gz
------------------------------------ wheel ------------------------------------
dist\pptx_md-0.1.0-py3-none-any.whl
```

경고/오류 없이 완료.

### pytest 전체 스위트 (AC1~AC7)

```
370 passed, 2 skipped, 25 warnings in 40.85s
Coverage: 92.15% (>= 75% gate)
test_docs.py: 7/7 PASS (AC1~AC7)
```

2 skipped: test_vector.py (ss) — 환경 의존성 스킵, 이슈 #45 무관.

### ruff check . && black --check .

```
All checks passed!
All done! 34 files would be left unchanged.
```

### mypy src/

```
Success: no issues found in 17 source files
```

---

## AC 커버리지

| AC | 커버 여부 | 비고 |
|----|---------|------|
| AC1 | 커버 (TC-45-01, TC-45-02) | PASS |
| AC2 | 커버 (TC-45-03, TC-45-04) | PASS |
| AC3 | 커버 (TC-45-05, TC-45-06) | PASS |
| AC4 | 커버 (TC-45-07, TC-45-08) | PASS |
| AC5 | 커버 (TC-45-09, TC-45-10) | PASS |
| AC6 | 커버 (TC-45-11, TC-45-12, TC-45-13) | PASS |
| AC7 | 커버 (TC-45-14, TC-45-15, TC-45-16) | PASS |

---

## 부기 — 별도 결함 (이슈 #45 범위 밖)

`test_description_pipeline.py::test_ac7_ruff_exit0`, `test_ac7_black_check_exit0` 2건이 1차 검증 시 간헐 실패(flaky) 관측되었음. 이번 재검증 실행에서는 전체 스위트 및 단독 실행 모두 PASS. 재현되지 않았으나 flaky 자체로 별도 이슈 등록 권고 (원인: 임시 파일 `tests/_ac4_tmp/snippet.py` 포맷 상태가 이전 테스트에 의존).

## RESULT: DONE

AC1~AC7 모두 통과. 이전 BLOCKED 사유(AC2 위반, _extract_code_blocks 버그) 해소 확인됨.
