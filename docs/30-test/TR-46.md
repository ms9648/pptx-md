## TR-46 FR-19 PyPI 배포 워크플로

실행: 2026-06-29 / 브랜치: feature/46-release
명령: `pytest tests/test_release.py -v` / `pytest` (전체 스위트)

| TC | 대응 AC | 시나리오 | 결과 |
|----|--------|---------|------|
| TC-46-01 | AC1 | hatch build → wheel 생성 | PASS |
| TC-46-02 | AC1 | hatch build → tarball 생성 | PASS |
| TC-46-03 | AC1 | wheel 파일명에 버전 0.1.0 포함 | PASS |
| TC-46-04 | AC1 | hatch build exit code 0 | PASS |
| TC-46-05 | AC2 | twine check dist/* PASSED | PASS |
| TC-46-06 | AC2 | pyproject.toml readme/license/version 비-placeholder | PASS |
| TC-46-07 | AC3 | release.yml 존재 + v* 태그 트리거 | PASS |
| TC-46-08 | AC3 | build/publish 잡 + publish needs build | PASS |
| TC-46-09 | AC3 | id-token: write (OIDC), 평문 토큰 0건 | PASS |
| TC-46-10 | AC3 | pypa/gh-action-pypi-publish 사용 | PASS |
| TC-46-11 | AC4 | publish 잡 environment: pypi | PASS |
| TC-46-12 | AC5 | QA 독자 venv → from pptx_md import convert (exit 0) | PASS |
| TC-46-13 | AC5 | branch push 트리거 부재 (tag-only 엄격성) | PASS |
| TC-46-14 | AC6 | QA 독자 core-only venv: anthropic/openai 없이 import pptx_md 성공 | PASS |
| TC-46-15 | AC6 | pyproject.toml core deps에 VLM SDK 없음, vlm extras 분리 | PASS |
| TC-46-16 | AC7 | release.yml/ci.yml 분리 + ci.yml pytest 스텝 확인 | PASS |
| TC-46-17 | AC7 | 전체 스위트 커버리지: 93.42% (게이트 75% 충족, exit 0) | PASS |
| TC-46-18 | AC7 경계 | test_release.py 단독 실행 시 커버리지 30% → exit 1 (예상 동작) | PASS |

### 실행 로그 증거

```
$ hatch build
  dist/pptx_md-0.1.0.tar.gz
  dist/pptx_md-0.1.0-py3-none-any.whl

$ python -m twine check dist/pptx_md-0.1.0-py3-none-any.whl dist/pptx_md-0.1.0.tar.gz
  Checking dist/pptx_md-0.1.0-py3-none-any.whl: PASSED
  Checking dist/pptx_md-0.1.0.tar.gz: PASSED

$ pytest tests/test_release.py -v
  27 passed, 6 warnings in 159.32s
  (coverage 30% — gate failure는 test_release.py 단독 실행의 예상 동작)

$ pytest (전체 스위트)
  410 passed, 2 skipped, 31 warnings in 244.87s
  Required test coverage of 75% reached. Total coverage: 93.42%

$ ruff check .     → exit 0 (All checks passed!)
$ black --check .  → exit 0 (36 files unchanged)
$ mypy src/        → exit 0 (Success: no issues found in 17 source files)

QA 독자 AC5: from pptx_md import convert → stdout: QA_IMPORT_OK (exit 0)
QA 독자 AC6: import pptx_md → stdout: QA_CORE_OK 0.1.0 (exit 0)
             anthropic absent: True / openai absent: True
```

### AC 커버리지 100%

| AC | 충족 여부 |
|----|---------|
| AC1 (hatch build → wheel+tarball, exit 0) | 충족 |
| AC2 (twine check PASSED, 메타데이터) | 충족 |
| AC3 (v* 트리거, OIDC, 평문 토큰 0건) | 충족 |
| AC4 (environment: pypi) | 충족 |
| AC5 (로컬 wheel install → import convert) | 충족 |
| AC6 (core-only import, NFR-08) | 충족 |
| AC7 (CI green 전제 구조) | 충족 |

### 관찰 사항 (NEXT)

1. **dist/ 구 아티팩트 잔류**: dist/ 에 pptx_md-0.0.0-*.whl, pptx_md-0.0.0.tar.gz가 남아 있음. AC 위반 아님 (0.1.0 아티팩트 존재로 충족). 배포 전 정리 권장.
2. **AC7 자동화 강제 수준**: release.yml build 잡이 pytest를 직접 실행하지 않음. CI green은 "사람이 태그 push 전 확인" 프로세스 게이트. workflow_run 기계적 강제 미적용. AC7 원문 취지 부합.
3. **environment: pypi 실효성**: YAML 선언 충족. 실제 GitHub Environment에 protection rules 등록은 사람 작업(ND-1) — 배포 전 필수.

### 결론

RESULT: DONE — 7개 AC 전부 PASS. 결함 없음.
