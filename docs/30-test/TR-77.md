## TR-77 FR-28 헤딩 계층 + TOC + 옵션 배선 (W-A)

실행: 2026-07-06 / 브랜치: `feature/77-fr28-heading-hierarchy` (PR #82, 검증은 `verify-77` 로 fetch)
환경 주의: 전역 site-packages 의 editable install(`.pth`)이 **다른 워크트리**(`agent-a5de300e44c29fba0`)를 가리키는 것을 발견(동시 병렬 에이전트 간 공유 site-packages 경합으로 추정). `pip install -e .` 만으로는 신뢰 불가하다고 판단해 **`PYTHONPATH=<본 워크트리>/src` 를 강제**하여 모든 명령을 재실행, `pptx_md.api.ConvertOptions` 필드 목록을 런타임으로 직접 확인해 브랜치 코드가 실제로 로드됨을 검증했다.

명령(전부 `PYTHONPATH=<worktree>/src` 하에서 실행):
- `python -m pytest -q --cov=src/pptx_md --cov-report=term-missing` (전체 회귀 + 커버리지)
- `python -m mypy src/`
- `python -m ruff check .` / `python -m black --check .`
- 독립 QA e2e 스크립트(비커밋, 스크래치패드) — 실제 `.pptx` 파일을 python-pptx 로 생성해 `convert()` 전체 파이프라인(parse→enrich→assemble)으로 AC1~AC6 재검증. developer 테스트는 전부 `assemble_document(손수 SlideIR)` 레벨이므로, `convert(source)` 진입점까지 실제로 옵션이 배선되는지는 별도 관찰 필요.

### 실행 결과 로그 요지
```
680 passed, 3 skipped, 59 warnings in ~203-296s
Required test coverage of 75% reached. Total coverage: 93.58%
mypy: Success: no issues found in 20 source files
ruff check .: All checks passed!
black --check .: All done! 46 files would be left unchanged.
```
스킵 3건은 본 이슈와 무관(COM 경로 N/A 1건, LibreOffice 미설치 2건 — 기존 환경 게이트).

### 신규/변경 라인 커버리지 (NFR-W3-2 ≥75%)
| 파일 | Stmts | Miss | Cover |
|------|-------|------|-------|
| `heading.py` (신규) | 15 | 0 | **100%** |
| `api.py` (+4필드) | 41 | 1 | 98% |
| `assembler.py` (리팩터+seam) | 270 | 25 | 91% |
| 전체 | 1230 | 79 | 93.58% |

`assembler.py` 미커버 라인(306-317, 326-328, 333-339, 349-351)은 슬라이드 렌더 예외격리 분기 및 `emit_frontmatter`/`include_notes` seam 이 **비어있는 문자열을 돌려주는 스텁**(`metadata.py`/`notes.py`, #78/#79 소관 미착수)일 때의 "non-empty 시 attach" 분기 — 스텁이 채워지기 전까지는 참(truthy) 경로에 도달할 수 없어 원천적으로 이 PR 범위에서 커버 불가. 이슈 #77 자체 범위(AC1~AC7)는 100%/91%로 임계치(75%) 상회.

### AC → TC 매트릭스 (이슈 #77 원문 AC1~AC7)

| TC | 대응 AC | 시나리오 | 결과 |
|----|--------|---------|------|
| TC-77-01 | AC1 (정상) | `heading.render_heading_lines`/`test_heading.py`: 2·3세그먼트 `>` 경로 → `##`/`###`/`####` 각 세그먼트 trim | PASS |
| TC-77-02 | AC1 (경계) | 7세그먼트 제목 → h6 클램프(6번째 이후 전부 `######`) | PASS |
| TC-77-03 | AC1 (독립 e2e) | 실제 `.pptx`(python-pptx 로 생성, 제목="Ⅱ. 사업의 이해 > ⑦ 현황·문제점 > 세부") + `convert(path, options=ConvertOptions(heading_hierarchy=True))` — 손수 IR 이 아닌 **실제 parse→assemble 전체 파이프라인**으로 3단계 헤딩 확인 | PASS |
| TC-77-04 | AC2 (정상) | 구분자 없는 제목 → 단일 `##`(`heading_hierarchy=True` 여도 미발동) | PASS |
| TC-77-05 | AC2 (경계) | 선행/후행 구분자만 있어 trim 후 세그먼트 1개(`">단일 세그먼트>"`) → 단일 `##`(원본 문자열 그대로, 분해 아님) | PASS |
| TC-77-06 | AC2 (독립 e2e) | 실제 `.pptx`(제목="단순 제목") + `convert(..., heading_hierarchy=True)` → 헤딩 라인 정확히 1줄 | PASS |
| TC-77-07 | AC3 (정상) | `heading_hierarchy=False`(기본) — `'>' ` 포함 제목도 원본 그대로 단일 `##`, off 명시/미지정 바이트 동일 | PASS |
| TC-77-08 | AC3 (경계) | 4옵션 전부 명시적 `False` == 4옵션 전부 미지정(`assemble_document(pres)`) 바이트 동일 | PASS |
| TC-77-09 | AC3 (독립 e2e, 예외적 조합) | `heading_hierarchy=False` 지만 `emit_toc`/`emit_frontmatter`/`include_notes` 는 켠 실제 변환 — 헤딩 분해가 **다른 3옵션과 독립적으로** 계속 미발동함을 확인(옵션 간 누출 없음) | PASS |
| TC-77-10 | AC4 (정상) | 실제 제목 + 빈 본문(도형 0개) → 헤딩만 유지된 블록(TOC 항목) | PASS |
| TC-77-11 | AC4 (경계) | fallback 제목(`## Slide N`) + 빈 본문 슬라이드가 문서 중간에 낀 3슬라이드 구성 → 해당 블록 완전 드롭, 문서 전체에 `"## Slide"` 문자열 0개 | PASS |
| TC-77-12 | AC4 (예외/회귀) | `emit_toc=False`(기본) → 레거시와 동일하게 빈 `## Slide N` 그대로 유지(회귀 없음 확인) | PASS |
| TC-77-13 | AC4 (독립 e2e) | 실제 `.pptx`: (a) 빈 레이아웃(제목 도형 없음) + `emit_toc=True` → 문서에 `"## Slide"` 없음, (b) 제목만 있는 실 슬라이드 + `emit_toc=True` → 문서가 정확히 `"## 섹션 표지"` 한 줄 | PASS |
| TC-77-14 | AC5 (정상) | `ConvertOptions()` 4신규필드 전부 기본값 `False`, frozen 유지(속성 재할당 시 `FrozenInstanceError`) | PASS |
| TC-77-15 | AC5 (독립 e2e, convert() 레벨) | 실제 `.pptx` + `convert(path)`(옵션 미지정) 무예외·`str` 반환 — developer 테스트는 `assemble_document` 레벨에서만 AC5 를 검증했으므로, `ConvertOptions()`/`convert(src)` 자체 하위호환을 **API 진입점에서 직접** 재확인 | PASS |
| TC-77-16 | AC6 (정상, 정적) | `heading.py`/`metadata.py`/`notes.py` 소스 텍스트에 `anthropic`/`openai`/`PIL`/`import pptx` 부재(정규식 매칭) | PASS |
| TC-77-17 | AC6 (독립 검증) | `heading.py` 자체 import 문 확인(`from __future__ import annotations` 뿐) + `metadata.py`→`ir.py` 의존 체인이 stdlib(`collections.abc`/`dataclasses`/`enum`) 로만 닫힘을 확인(패키지 `__init__.py` 를 통한 간접 로드가 아닌 **모듈 자체 소스**·**직접 의존 체인** 기준) | PASS |
| TC-77-18 | AC7 (정상) | `mypy src/`/`ruff check .`/`black --check .`/`pytest -q` 4개 게이트 독립 재실행, 전부 exit 0 | PASS |

**AC 커버리지: 이슈 #77 원문 AC1~AC7 100%.**

### developer 단위 테스트 충실성 점검 (형식적 테스트 여부)
`tests/test_heading.py`(신규, AC1~AC4 순수함수 단위), `tests/test_assembler.py` 의 `TestAc1~TestAc7` 클래스를 열람. 전량 실제 AC 의 Then 을 검증하는 실질 단언(예: `test_ac1_two_segment_title_h2_h3` 은 헤딩 라인 텍스트를 직접 비교, `test_ac4_fallback_title_empty_body_dropped` 는 `"## Slide"` 문자열 부재를 직접 검사)이며 `assert True`/속성 존재 확인류 형식적 테스트는 발견되지 않았다. `TestAc7QualityGates` 가 `subprocess` 로 `mypy`/`ruff`/`black` 을 pytest 안에서 재호출하는 방식은 실행 시간 증가(스위트 전체 ~203~296s 중 상당 부분 기여) 요인이나 결함은 아니며, PM/향후 리뷰 시 "환경 의존 subprocess 테스트를 CI 별도 스텝과 중복 실행" 여부만 참고용으로 남긴다(RISKS 참조).

### AC5 관련 참고 (결함 아님, 관찰)
developer 의 AC-증거 매핑표는 AC5(`ConvertOptions()`·`convert(src)` 무손상)를 `tests/test_assembler.py::TestAc5OptionsBackwardCompatible`(assemble_document 레벨) + "전체 `tests/test_api.py` 통과(회귀 0)"로 매핑했다. `test_api.py` 자체에는 FR-28 신규 4필드를 명시적으로 검증하는 전용 테스트가 없어(간접 회귀만) 다소 약한 증거였으나, 본 검증에서 TC-77-15(독립 e2e, `convert()` 진입점 직접 호출)로 갭을 메워 AC5 는 최종 PASS 로 판정한다. 결함 아님 — NEXT 로만 기록.

### 결정성/회귀 안전성 (참고, 본 이슈 AC 범위 외)
`heading_hierarchy`/`emit_toc` 로직은 순수 함수 조합(`str.split`/인덱스 순회)이라 별도 난수·set/dict 순회 의존이 없음을 코드 열람으로 확인. 결정성 게이트(AC10 상당)는 FR-28 전체를 다루는 후속 Reviewer 이슈(#골든 게이트, FR-29 AC8~AC10) 소관이므로 본 TR 범위에서는 판정하지 않는다.

### 결함
없음. 4개 품질 게이트(pytest/mypy/ruff/black) 전부 exit 0, AC1~AC7 전부 PASS.
