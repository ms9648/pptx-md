## TR-78 FR-28 frontmatter/슬라이드 메타 (metadata.py, W-B)

실행: 2026-07-10 / 브랜치: `feature/78-fr28-metadata` (PR #85, HEAD `0be1d9554616bffa4e6167732d6bf467bd442a2f`) / 검증 워크트리: `agent-a8653eb3924f4e760`

환경 주의(TR-77/TR-80 선례 재확인): `python -c "import pptx_md; print(pptx_md.__file__)"` 를 `PYTHONPATH` 미설정 상태로 실행하면 **다른 병렬 워크트리**(`agent-a4b488d3773a346ac`)의 editable-install 경로가 나와, 전역 site-packages `.pth` 경합이 이번에도 재현됨을 확인. 이후 전 명령을 `PYTHONPATH=<본 워크트리>/src` 강제 하에 재실행하고, 런타임으로 `pptx_md.__file__` 이 본 워크트리를 가리킴을 재확인한 뒤 진행했다.

### 게이트 재현 (developer 테스트 전체 재실행 + 독립 재확인)
명령(전부 `PYTHONPATH=<worktree>/src` 하에서 실행):
```
python -m pytest -q                                              # 704 passed, 3 skipped, 218.22s
python -m pytest -q --cov=src/pptx_md --cov-report=term-missing   # Total coverage: 94.59% (요구 75%↑)
python -m ruff check .                                            # All checks passed!
python -m black --check .                                         # 47 files would be left unchanged.
python -m mypy src/                                                # Success: no issues found in 20 source files
```
스킵 3건은 본 이슈와 무관(COM 경로 N/A 1건, LibreOffice 미설치 2건 — 기존 환경 게이트, TR-77/TR-80과 동일 사유).

`metadata.py` 신규/변경 라인 커버리지(NFR-W3-2 ≥75%): **20 stmts, 0 miss, 100%**.

### 독립 QA 시나리오 (developer 테스트 재실행과 별개, 스크래치패드에서 실행 — 리포지토리 미커밋)
경로: `C:\Users\ms964\AppData\Local\Temp\claude\D--dev-pptx-md\8687d0f1-2d89-47c4-bb16-b4e53c22d3c7\scratchpad\qa_tr78.py` (18개 테스트, 전부 `convert()`/`assemble_document()` 진입점을 통한 실제 파이프라인 또는 developer 테스트가 다루지 않은 조합/경계를 대상으로 새로 작성).

실행 결과:
```
python -m pytest qa_tr78.py -v
18 passed, 2 warnings in 2.25s
```
(`2 warnings` 는 Pillow `getdata` deprecation — 본 이슈와 무관, 테스트 픽스처의 실제 이미지 생성 경로에서만 발생)

### AC → TC 매트릭스 (이슈 #78 원문 AC1~AC7)

| TC | 대응 AC | 시나리오 | 결과 |
|----|--------|---------|------|
| TC-78-01 | AC1 (정상) | 실 3슬라이드 `.pptx` + `convert(path, ConvertOptions(emit_frontmatter=True))` → 문서 최상단 `---\ngenerator: pptx-md\nslide_count: 3\n---\n\n` | PASS |
| TC-78-02 | AC1 (경계) | 0슬라이드 실 `.pptx` → `slide_count: 0`, 슬라이드 주석/본문 0개, 문서가 frontmatter 뿐 | PASS |
| TC-78-03 | AC1 (예외/PII) | 소스 경로에 민감 문자열("김철수_기밀문서") 포함 → frontmatter 및 전체 출력에 경로/PII 미노출(`source_path` 키 자체도 부재) | PASS |
| TC-78-04 | AC2 (정상) | 실 3슬라이드(제목 Overview/Body/Conclusion) → 각 주석 `slide_index` 1,2,3 순차 + `section` 이 실제 제목과 정확히 일치 | PASS |
| TC-78-05 | AC2 (경계) | 실 슬라이드 제목에 `\|` 포함("매출 \| 비용") → 주석 내 `/` 치환, 주석 라인 전체의 `\|` 개수가 구조적 2개로 고정 | PASS |
| TC-78-06 | AC2 (예외) | 실 제목 텍스트박스 2단락(개행 포함) → 주석이 여전히 단일 물리 라인(개행이 공백으로 치환됨을 실제 파이프라인에서 재확인) | PASS |
| TC-78-07 | AC3 (정상, 실제 분류기 경유) | 실제 그리드형 다이어그램 PNG(Pillow 생성, 실제 `classify_image` 로 DIAGRAM 판정) 포함 슬라이드 + 노이즈 사진 PNG(PHOTO 판정) 포함 슬라이드 → `convert()` 전체 파이프라인에서 `has_diagram: true`/`false` 정확히 분기 (developer 테스트는 classification 수동 지정만 수행 — 본 TC 는 FR-06 실제 분류기까지 경유) | PASS |
| TC-78-08 | AC3 (경계) | 그룹 안의 그룹(2단계 중첩) 내부 DIAGRAM 이미지 → `has_diagram: true` (developer 테스트는 1단계 중첩까지만 커버) | PASS |
| TC-78-09 | AC3 (예외) | `classification=None`(미분류) 이미지 → `has_diagram: false`, 무예외 | PASS |
| TC-78-10 | AC4 (정상) | 실 3슬라이드(Alpha/Beta/Gamma) 연속 처리 → 각 슬라이드 주석의 `section` 이 오직 자신의 제목만 반영(이전 슬라이드 section 누출 없음 — 슬라이드 간 상태 격리, developer 단위 테스트는 단일 `heading_lines` 리스트만 다뤄 이 시나리오 미검증) | PASS |
| TC-78-11 | AC4 (경계) | 제목 placeholder 는 존재하나 텍스트가 빈 문자열 → 실제 파이프라인에서 fallback 제목("Slide 1")으로 처리되고 `section` 도 그 fallback 텍스트를 반영(빈 문자열 아님) — 설계 문서상 암묵적으로만 규정된 지점을 실측하여 확인 | PASS |
| TC-78-12 | AC4 (예외적 조합) | `heading_hierarchy=True` + `emit_frontmatter=True` 동시 활성, 제목="보고서 > 세부내용" → `section` 은 최상위(첫) 세그먼트("보고서")만 반영, 원본 전체 제목 미반영(두 옵션의 상호작용은 developer 테스트에서 직접 조합되지 않음) | PASS |
| TC-78-13 | AC5/AC7 (정상) | 실 4슬라이드(제목+본문) → `emit_frontmatter` on/off 각각 `validate_markdown` 경고 수 비교, on 이 off 이하(증가 없음), `valid` 동일 | PASS |
| TC-78-14 | AC5 (경계) | 슬라이드 1개(슬라이드 구분자 `---` 가 전혀 발생하지 않는 상태) → 문서 전체의 독립 `---` 라인이 YAML open/close 2개뿐 | PASS |
| TC-78-15 | AC5 (예외) | 실 슬라이드 제목이 리터럴 `"---"` → 헤딩 라인이 `"## ---"` 로 렌더되어 phantom bare `---` 라인 미생성(구분자/파서 오탐 방지), on/off 경고 수 동일 | PASS |
| TC-78-16 | AC6 (정상) | 실 파일 1회 파싱한 `PresentationIR` 을 `assemble_document(emit_frontmatter=True)` 로 2회 호출 → 슬라이드 제목/도형 리스트 불변(IR 무변경) | PASS |
| TC-78-17 | AC6 (경계, 관찰) | 별도 서브프로세스에서 `import pptx_md.metadata` → `sys.modules` 에 `pptx`/`PIL` 존재(리크 관찰). 원인은 `pptx_md/__init__.py` 가 서브모듈 임포트 시 항상 먼저 실행되며 그 안에서 `pptx_md.api` 를 즉시 로드하기 때문(패키지 초기화 의미론, ADR-603 — #78 이전부터 존재, `metadata.py` 자신의 import 문과 무관). AC6/INV-5 의 실제 의도(§3.3/§3.4: "metadata.py 최상단은 stdlib+내부 ir 만")는 developer 의 AST 기반 정적 검사(모듈 자신의 소스만 검사)로 이미 충족되고, TR-77(TC-77-17)도 동일 경계를 의도적으로 회피한 선례가 있어 본 TC 는 AC6 FAIL 판정 근거로 쓰지 않고 관찰(RISKS)로만 기록 | PASS(관찰 스냅샷) |
| TC-78-18 | AC6/INV-1 (예외, 결정성) | 동일 실 `.pptx` 를 `convert(emit_frontmatter=True)` 로 2회 변환 → 바이트 동일 | PASS |
| TC-78-19 | AC7 (정상) | `pytest`/`--cov`/`ruff check .`/`black --check .`/`mypy src/` 5개 게이트 독립 재실행, 전부 exit 0 (위 "게이트 재현" 절 로그 참조) | PASS |

**AC 커버리지: 이슈 #78 원문 AC1~AC7 100%.**

### developer 단위 테스트 충실성 점검 (형식적 테스트 여부)
`tests/test_metadata.py`(신규 24건, `TestAc1BuildFrontmatter`~`TestAc6IrUnchangedAndImportIsolation`)를 전건 열람. 전량 실제 AC 의 Then 을 직접 검증하는 실질 단언이었다 — 예: `test_ac1_frontmatter_excludes_source_path` 는 PII 미포함을 문자열 부재로 직접 확인, `test_ac2_comment_sanitises_pipe_in_section` 은 치환 결과와 구조적 파이프 개수(`count("|") == 2`)까지 검사, `test_ac6_module_imports_only_stdlib_and_ir` 은 `ast.parse` 로 실제 import 트리를 순회해 금지 모듈 부재를 확인. `assert True`/속성 존재 확인류 형식적 테스트는 발견되지 않았다. `TestAc5NoConflictWithSeparatorsOrHeadings` 클래스가 `assemble_document()` 를 통해 통합 수준까지 검증한 점도 확인(순수 함수 단위 테스트에 그치지 않음).

### AC6 관련 관찰 (결함 아님, TR-77 선례와 동일 성격)
TC-78-17 참조. `pptx_md` 패키지 최상위 `__init__.py`(이슈 #78 편집 범위 밖, ADR-603)의 즉시 로딩 구조상 `import pptx_md.metadata` 단독으로도 `pptx`/`PIL` 이 프로세스에 로드된다. `metadata.py` 자신의 소스는 `from __future__ import annotations` 와 `from pptx_md.ir import ...` 뿐이며(직접 열람으로 재확인), AC6/INV-5 가 의도하는 검증 범위(모듈 자신의 최상단 import)를 벗어나는 관찰이므로 FAIL 판정에는 반영하지 않는다. RISKS 로만 기록.

### 결정성/회귀 안전성
`build_frontmatter`/`build_slide_comment`/`derive_has_diagram`/`derive_section` 전부 순수 함수(부작용·난수·set/dict 순회 의존 없음)이며, TC-78-18 로 `convert()` 진입점 수준의 결정성(INV-1)까지 재확인했다.

### 결함
없음. 5개 품질 게이트(pytest/coverage/mypy/ruff/black) 전부 exit 0, 독립 QA 18건 + 게이트 재현 1건 = TC-78-01~19 전부 PASS, 이슈 #78 AC1~AC7 전건 커버.

### RISKS
- TC-78-17 관찰(패키지 `__init__.py` 즉시 로딩으로 인한 전이적 `pptx`/`PIL` 로드)은 #78 범위 밖이며 결함이 아니지만, 향후 "메타데이터 전용 CLI/lightweight import" 같은 요구가 생기면 `pptx_md/__init__.py` 지연 임포트화가 별도 개선 과제가 될 수 있음(NEXT 참고, TR-77 에서도 동일 관찰 언급 없었으나 본 TR 에서 최초로 실측·기록).

### RESULT
DONE
