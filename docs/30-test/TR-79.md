## TR-79 FR-28 발표자 노트 블록 (notes.py, W-C)

실행: 2026-07-10 / 브랜치: `feature/79-fr28-notes` (PR #84, HEAD `c9a2dbf`, base `9a24d4c` = PR #82 머지 후)
환경 주의: 병렬 에이전트 간 전역 site-packages editable 포인터 경합을 피하기 위해 `pip install -e .` 를 사용하지 않고, 격리 worktree(`agent-a442ceeb179704cb2`)에서 `git fetch origin feature/79-fr28-notes && git checkout FETCH_HEAD` 후 **`PYTHONPATH=<worktree>/src` 를 강제**했다. `python -c "import pptx_md; print(pptx_md.__file__)"` 로 로드 경로가 본 worktree 임을 먼저 확인.

명령(전부 `PYTHONPATH=<worktree>/src` 하에서 실행):
- `python -m pytest -q` (전체 회귀)
- `python -m pytest --cov=src/pptx_md --cov-report=term-missing -q` (커버리지)
- `python -m ruff check .` / `python -m black --check .`
- `python -m mypy src/`
- 독립 QA 통합 스크립트(비커밋, 스크래치패드 `qa_test_79.py`) — `render_notes_block` 단독 호출뿐 아니라 **`assemble_document(include_notes=...)` 실 seam 경로**(assembler.py:325-328)를 통해 멀티슬라이드 문서 조립까지 검증. developer 의 `tests/test_notes.py` 는 `render_notes_block` 만 단위 테스트하므로, seam 통합·`heading_hierarchy` 병행 옵션과의 상호작용은 이 스크립트로 독자 커버.

### 실행 결과 로그 요지 (독립 재현)
```
$ python -m pytest -q --cov=src/pptx_md --cov-report=term-missing
...
src\pptx_md\notes.py                      9      0   100%
TOTAL                                   1235     78    94%
Required test coverage of 75% reached. Total coverage: 93.68%
692 passed, 3 skipped, 59 warnings in 204.00s (0:03:23)   [1차]
692 passed, 3 skipped, 59 warnings in 136.74s (0:02:16)   [2차, 확인 재실행]

$ python -m ruff check .
All checks passed!

$ python -m black --check .
All done! (✨) 47 files would be left unchanged.

$ python -m mypy src/
Success: no issues found in 20 source files
```
스킵 3건은 COM/LibreOffice 환경 게이트로 본 이슈와 무관(기존 환경 의존). developer REPORT(692 passed / notes.py 100%)와 독립 재현 수치 일치.

### diff 범위 확인 (소유권 격리)
`git diff --name-status 9a24d4c..HEAD`:
```
M  src/pptx_md/notes.py
A  tests/test_notes.py
```
`assembler.py`/`api.py` 미접촉 확인(#77 seam 선배선 그대로 재사용).

### 신규 라인 커버리지 (NFR-W3-2 ≥75%)
| 파일 | Stmts | Miss | Cover |
|------|-------|------|-------|
| `notes.py` (신규 본문) | 9 | 0 | **100%** |

### AC → TC 매트릭스 (이슈 #79 원문 AC1~AC5, 정상/경계/예외 각 1+)

| TC | 대응 AC | 시나리오 | 결과 |
|----|--------|---------|------|
| TC-79-01 | AC1 (정상) | 단일줄 notes + `assemble_document(include_notes=True)` **실 seam 통합** → 슬라이드 블록 말미에 `> notes\n> …` 부착 | PASS |
| TC-79-02 | AC1 (경계) | 3슬라이드(notes 있음/없음/있음 혼합) → 정확히 2개 블록만 부착, 슬라이드 인덱스 순서 보존(`> notes` 카운트==2) | PASS |
| TC-79-03 | AC1 (예외/견고성) | 50,000자 단일줄 notes → 트렁케이션 없이 전체 보존(`.startswith` + 원문 포함 확인) | PASS |
| TC-79-04 | AC2 (정상) | `notes==""` + `include_notes=True` → 블록 미부착, **`include_notes=False` 문서와 바이트 완전 동일**(off-path parity — developer 테스트 미검증 지점) | PASS |
| TC-79-05 | AC2 (경계) | ASCII 공백/탭/개행만(`"   \n\t  "`) → `""` | PASS |
| TC-79-06 | AC2 (경계, 유니코드) | 전각공백(`　`)+NBSP(`\xa0`)+ASCII 공백 혼합만 → `""`(Python `str.strip()` 이 비-ASCII 공백도 커버함을 확인) | PASS |
| TC-79-07 | AC2 (반증/경계선 정확성) | 유니코드 공백 사이 비공백 문자 1개(`"　내용　"`) → 블록 **생성**됨(공백 판정 임계값이 딱 `strip()`빈문자열 여부이지 과잉 매칭 아님을 검증) | PASS |
| TC-79-08 | AC3 (정상) | `"첫줄\v둘째줄"` + seam 통합 → `\v`가 `\n`으로 정규화되어 별도 줄로 렌더 | PASS |
| TC-79-09 | AC3 (경계) | 선행/후행 `\v`(`"\v중간\v"`) → 빈 선행/후행 줄도 `> ` 프리픽스로 생성(줄 드롭 없음) | PASS |
| TC-79-10 | AC3 (예외/혼재) | `\v`와 기존 `\r\n`(CRLF) 혼재 → `\v`만 정규화, `\r`은 그대로 보존(ADR-618 범위 = `\v→\n` 한정, 회귀 아님을 확인) | PASS |
| TC-79-11 | AC4 (정상) | 3슬라이드(계층 제목·빈 notes·`\v` 포함 notes 혼합) + `include_notes=True, heading_hierarchy=True` 조합 → 2회 조립 바이트 동일(결정성, 옵션 간 상호작용 포함) | PASS |
| TC-79-12 | AC4 (경계/견고성) | `\v`×5000, 이모지+한글 혼재, CRLF+VT 혼재, `"a\v"`×2000 등 경계 입력 4종 → 전부 무예외 | PASS |
| TC-79-13 | AC4 (정적 검증, 예외 케이스 보강) | `ast` 모듈로 `notes.py` 의 `Import`/`ImportFrom` 노드를 **직접 파싱**(문자열 부분매칭 아님)해 forbidden root(`anthropic`/`openai`/`pptx`/`PIL`) 0 확인 — developer 테스트가 substring 매칭이라 `from pptx import ...` 형태를 놓칠 수 있다고 리뷰어가 지적한 갭을 AST 기준으로 재검증 | PASS |
| TC-79-14 | AC5 (게이트) | `pytest`/`ruff check .`/`black --check .`/`mypy src/` 4개 게이트 독립 재실행, 전부 exit 0. `notes.py` 신규 라인 100% 커버리지(≥75% 기준 충족) | PASS |

**AC 커버리지: 이슈 #79 원문 AC1~AC5 100%.**

### developer 단위 테스트 충실성 점검 (형식적 테스트 여부)
`tests/test_notes.py`(`TestAc1RenderNotesBlock`~`TestAc4DeterministicAndIsolated`)를 열람. 전량 정확한 기대 문자열 `==` 단언(예: `test_ac1_single_line_notes_renders_header_and_line` 이 `"> notes\n> 발표자 메모입니다"` 를 직접 비교) — `assert True`/속성 존재 확인류 형식적 테스트는 발견되지 않았다. 다만 `test_ac4_no_forbidden_imports_in_module_source` 가 `f"import {name}"` **문자열 부분매칭**이라 `from pptx import ...` 형태의 import 는 탐지하지 못하는 약점이 있음(리뷰어 코멘트와 동일 지적). 실 코드에는 `from __future__` 외 import 가 없어 AC4 자체는 위반되지 않으나, 테스트의 견고성 자체는 약하다 — 본 검증에서 TC-79-13(AST 기반 정적 감사)로 독립 재확인해 갭을 메웠다. **결함 아님 — NEXT 로 기록.**

### 결정성/회귀 안전성
`assemble_document(include_notes=True, heading_hierarchy=True)` 조합 2회 조립 바이트 동일 확인(TC-79-11) — `heading.py`(#77)와의 상호작용에서도 `include_notes` 가 결정적으로 동작함을 developer 테스트 범위(단일 옵션) 밖에서 추가 검증.

### 결함
없음. 4개 품질 게이트(pytest/mypy/ruff/black) 독립 재실행 전부 exit 0, AC1~AC5 전부 PASS, `notes.py` 신규 라인 100% 커버리지. `assembler.py`/`api.py` 미접촉(소유권 격리) 확인.

### RESULT: DONE
