# TR-80 Validator 신규 4규칙 (표/중복헤딩/제어문자/빈슬라이드) [W-D]

> 대상: PR #83 / 브랜치 `feature/80-fr29-validator-rules` / 검증 HEAD `16bfdd1`(dd08c0b 구현 + 16bfdd1 developer측 자체 TR 커밋 포함, 본 리포트로 master 표준 산출물 박제)
> 요구 정본: `docs/10-requirements/REQ-wave3.md` §3 FR-29 AC1~AC7, NFR-W3-2(신규 라인 커버리지 ≥75%)
> 설계 정본: `docs/20-design/ARCH-Wave3.md` §3.7, ADR-619
> 수용 기준(이슈 #80 본문): AC1~AC8
> 실행: 2026-07-10 / 격리 worktree 독립 검증
> 명령: `pytest -q`, `pytest --cov=src/pptx_md --cov-report=term-missing`, `ruff check . && black --check .`, `mypy src/`

## 0. 검증 환경 및 방법론 주의사항

격리 worktree 에서 `git fetch origin feature/80-fr29-validator-rules && git checkout FETCH_HEAD` 로 PR 브랜치를 체크아웃(HEAD `16bfdd1`). `pip install -e . --no-deps` 실행 직후 `python -c "import pptx_md; print(pptx_md.__file__)"` 로는 본 worktree 경로가 확인되었으나, **직후 재확인 시 전역 site-packages 의 editable 포인터가 다른 워크트리(`agent-a4b488d3773a346ac`)로 바뀌어 있음을 발견**(동시 병렬 에이전트가 같은 site-packages 를 공유하며 `pip install -e .` 를 경합적으로 재실행하는 환경 특성 — 과거 TR-77 에서도 동일 현상 관찰됨). 이 상태에서는 `python -c`/개별 스크립트 검증이 **엉뚱한 브랜치의 코드를 검사할 위험**이 있어, 신뢰성 확보를 위해 이후 모든 게이트 명령을 `PYTHONPATH=<본 worktree>/src` 로 강제하고, 매 실행 전 `pptx_md.__file__` 이 본 worktree 를 가리키는지 재확인했다. (참고로 `pytest -q` 전체 스위트 자체는 강제 전/후 결과가 동일했다 — pytest 수집 시점의 임포트가 마침 올바른 경로를 잡았던 것으로 보이나, 개별 `python -c` 프로브는 실제로 다른 코드를 검사하고 있었으므로 이 강제 없이는 독립 프로브 결과를 신뢰할 수 없었다.)

## 1. 전체 스위트 결과 (PYTHONPATH 강제, 재현 가능)

```
$ PYTHONPATH=<worktree>/src python -m pytest -q
677 passed, 3 skipped, 63 warnings in 163.83s (0:02:43)
Required test coverage of 75% reached. Total coverage: 95.20%
```

- 실패 0건. `tests/test_validator.py` 는 developer 신규 클래스(`TestFr29Ac1~Ac7`, 18건) + 이전 검증 라운드에서 QA 가 브랜치에 보강한 독립 클래스(`TestFr29QaSupplementIndependent`, 8건) + 기존 FR-14 8클래스(45건) = 71건 전건 통과, 기존 스위트 회귀 0.

## 2. AC → 테스트 케이스 매트릭스 (이슈 #80 AC1~AC8, 커버리지 100%)

developer 테스트(63건 중 FR-29분 18건) + 브랜치에 이미 보강된 QA 독립 테스트(8건) + 본 라운드에서 **신규로 실행한 추가 독립 프로브**(TC-80-37~46, 스크래치패드 비커밋 스크립트, `validate_markdown` 직접 호출)로 3중 검증했다.

| TC | 대응 AC | 시나리오 | 출처 | 결과 |
|----|--------|---------|------|------|
| TC-80-01 | AC1 (정상) | 헤더+구분행만, 데이터 행 0 → "깨진 표" 경고 1건, valid 불변 | developer | PASS |
| TC-80-02 | AC1 (예외) | 코드펜스 내부 `\|` 라인 → 표 오인 없음(경고 0) | developer | PASS |
| TC-80-03 | AC1 (대조) | 데이터 있는 정상 표 → 깨진 표 경고 없음 | developer | PASS |
| TC-80-04 | AC1 (경계) | 2번째 행이 구분행 아님 → 표 미확정(경고 없음) | developer | PASS |
| TC-80-05 | AC1 (경계, QA) | 헤더 셀 내 이스케이프 파이프(`\|`)가 열 계산을 깨지 않음 | QA(브랜치 보강) | PASS |
| TC-80-37 | AC1 (독립 신규) | 문서 내 표 2개 — 하나는 데이터0(깨짐), 하나는 정상 → 경고 정확히 1건(정상 표는 미발동, 교차 오염 없음) | QA(본 라운드) | PASS |
| TC-80-38 | AC1 (독립 신규, 경계) | GFM 정렬 콜론 구분행(`:---`/`---:`/`:---:`) → 정상 표로 인식(오탐 없음) | QA(본 라운드) | PASS |
| TC-80-06 | AC2 (정상) | 데이터 행 열 수 < 헤더 → 열 수 불일치 경고 1건 | developer | PASS |
| TC-80-07 | AC2 (경계) | 데이터 행 열 수 > 헤더 → 열 수 불일치 경고 1건 | developer | PASS |
| TC-80-08 | AC2 (대조) | 열 수 일치 → 경고 없음 | developer | PASS |
| TC-80-09 | AC2 (경계, QA) | 열 불일치 데이터 행이 여러 개여도 표당 경고는 1건(과다 경고 방지) | QA(브랜치 보강) | PASS |
| TC-80-39 | AC2 (독립 신규, 예외) | 헤더는 leading/trailing pipe 있고 데이터행은 pipe 스타일이 다름(파이프 개수 상이) → 열 불일치 정상 감지 | QA(본 라운드) | PASS |
| TC-80-10 | AC3 (정상) | 인접(사이 본문 없음) 동일 텍스트 헤딩 2줄 → 중복 인접 헤딩 경고 1건 | developer | PASS |
| TC-80-11 | AC3 (대조) | 사이에 본문 있으면 인접 아님 → 경고 없음 | developer | PASS |
| TC-80-12 | AC3 (대조) | 사이에 `---` 슬라이드 구분자 → 인접 아님 → 경고 없음 | developer | PASS |
| TC-80-13 | AC3 (대조) | 텍스트 다르면 인접해도 경고 없음 | developer | PASS |
| TC-80-15 | AC3 (경계, QA) | 동일 헤딩 사이 HTML 주석(FR-28 메타) 개입 → 인접 아님(경고 없음) | QA(브랜치 보강) | PASS |
| TC-80-40 | AC3 (독립 신규, 경계) | 사이에 **공백 줄만** 있는 동일 텍스트 헤딩 2줄 → 여전히 인접 판정(공백 줄은 "본문"이 아님, 경고 발생) — 설계 §3.7 "비공백 라인" 기준과 일치 | QA(본 라운드) | PASS |
| TC-80-41 | AC3 (독립 신규, 예외) | 텍스트는 동일하나 헤딩 레벨이 다른 인접 헤딩(`## Foo`→`### Foo`) → 레벨 무관 텍스트 일치로 경고 발생(설계상 "동일 텍스트" 기준, 레벨 비교 언급 없음 — 결함 아님, 설계 그대로) | QA(본 라운드) | PASS |
| TC-80-16 | AC4 (정상) | `\v` 1개 잔존 → 경고에 "1개" 포함 | developer | PASS |
| TC-80-17 | AC4 (경계) | `\v` 3개 잔존 → 경고에 "3개" 포함 | developer | PASS |
| TC-80-18 | AC4 (대조) | `\v` 없음 → 경고 없음 | developer | PASS |
| TC-80-42 | AC4 (독립 신규, 경계) | 코드펜스 **내부**의 `\v` 도 카운트에 포함(설계 §3.7 `_check_control_chars` 는 펜스 제외 언급 없이 `md.count("\v")` 전역 카운트 — 구현이 설계와 일치, 결함 아님) | QA(본 라운드) | PASS |
| TC-80-19 | AC5 (정상) | 모든 셀 공백인 pipe table → 전공백 표 경고 1건 | developer | PASS |
| TC-80-20 | AC5 (대조) | 일부 셀 내용 있음 → 전공백 경고 없음 | developer | PASS |
| TC-80-43 | AC5 (독립 신규, 조합) | 열 수 불일치 + 전공백이 같은 데이터 행에서 동시 발생 → 두 경고 모두 독립 발생(상호 배제 아님, AC 문구상 배타 요구 없음 — 결함 아님) | QA(본 라운드) | PASS |
| TC-80-21 | AC6 (정상) | 헤딩만 있고 본문 없는 슬라이드 블록(중간) → 빈 슬라이드 경고 1건 | developer | PASS |
| TC-80-22 | AC6 (경계) | 헤딩+HTML 주석만 있는 블록도 빈 슬라이드로 판정 | developer | PASS |
| TC-80-23 | AC6 (대조) | 모든 블록에 본문 있음 → 경고 없음 | developer | PASS |
| TC-80-24 | AC6 (경계) | 연속 `---` 사이 0줄 블록도 빈 슬라이드로 판정 | developer | PASS |
| TC-80-25 | AC6 (경계) | frontmatter 앞 공백 줄 있어도 정상 스킵(오탐 없음) | developer | PASS |
| TC-80-26 | AC6 (경계, QA) | 문서 맨 앞(첫 `---` 이전) 슬라이드 블록이 비어도 빈 슬라이드 경고 | QA(브랜치 보강) | PASS |
| TC-80-27 | AC6 (예외, QA) | 헤딩 뒤 펜스 코드블록만 있는 슬라이드는 본문으로 인정(오탐 없음) | QA(브랜치 보강) | PASS |
| TC-80-44 | AC6 (독립 신규, 경계) | 문서 마지막 블록이 `---` 뒤 공백 줄만 있고 그대로 EOF(후행 개행/구분자 없음) → 빈 슬라이드 경고 정상 발생 | QA(본 라운드) | PASS |
| TC-80-28 | AC7 (정상) | 결함 없는 정상 문서 → 신규 경고 0, valid=True | developer | PASS |
| TC-80-29 | AC7 (회귀) | 기존 FR-14 헤딩 시작 경고는 유지, 신규 규칙과 혼입 없음 | developer | PASS |
| TC-80-30 | AC7 (예외) | 기형적 pipe 콘텐츠(`\|\|\|\|\|`) → 무예외, `ValidationResult` 반환 | developer | PASS |
| TC-80-31 | AC7 (경계) | YAML frontmatter 블록은 표/빈 슬라이드 분석에서 제외(오탐 없음) | developer | PASS |
| TC-80-32 | AC7 (예외, QA) | 기존 미닫힘 펜스(valid=False)와 신규 규칙 공존 시 valid 는 기존 규칙에만 좌우 | QA(브랜치 보강) | PASS |
| TC-80-33 | AC7 (예외, QA) | frontmatter 닫는 구분자 없는 기형 입력 → 무예외(fallback 라인 238 커버) | QA(브랜치 보강) | PASS |
| TC-80-45 | AC7 (독립 신규, 정상) | 표+헤딩+`---`구분자 모두 정상인 복합 문서(§3.7 대상 4규칙 전부 no-trigger 조건 동시 충족) → 신규 경고 0 재확인(개별 케이스가 아닌 통합 문서 레벨 회귀 확인) | QA(본 라운드) | PASS |
| TC-80-46 | AC7 (독립 신규, 예외) | 표 2개가 빈 줄 없이 연속(런 병합 상황) → 무예외, 허위 경고 폭증 없음(런 경계 처리 견고성 확인) | QA(본 라운드) | PASS |
| TC-80-34 | AC8 | `ruff check . && black --check .` exit 0 | QA(본 라운드, PYTHONPATH 무관 — 파일 기반) | PASS |
| TC-80-35 | AC8 | `mypy src/` exit 0(PYTHONPATH 강제 재확인) | QA(본 라운드) | PASS |
| TC-80-36 | AC8 | `pytest -q` 전체 스위트 실패 0(PYTHONPATH 강제 재확인, 677 passed/3 skipped) | QA(본 라운드) | PASS |

**AC 커버리지: 이슈 #80 AC1~AC8 전건 100%.** AC1~AC7 각각 정상/경계/예외 케이스 최소 1개 이상 확보(TC-80-37~46 은 본 라운드에서 developer/기존 QA 테스트와 다른 조합으로 새로 설계·실행한 독립 프로브).

## 3. developer 단위 테스트 "형식적 테스트" 여부 점검

`TestFr29Ac1~Ac7`(18건)를 재열람. 전부 `validate_markdown()` 의 실제 반환값(`warnings` 문자열 substring·건수, `valid` 불변)을 직접 단언하며 mock 검증·속성 존재 확인류 형식적 패턴은 없음. AC 마다 발동 케이스 + 최소 1개 대조(미발동) 케이스가 짝을 이뤄 회귀 방지력이 있다고 판단. 브랜치에 이미 추가된 `TestFr29QaSupplementIndependent`(8건)도 동일 기준으로 실질적이며, developer 스위트가 다루지 않은 경계(첫 슬라이드 블록/펜스 콘텐츠/이스케이프 파이프/다중 케이스/HTML 주석 개입/frontmatter fallback/`valid` 조합)를 메운 것으로 확인.

본 라운드에서 추가로 developer/기존 QA 테스트와 겹치지 않는 10개 시나리오(TC-80-37~46)를 별도 스크립트로 직접 실행해 3중 검증했으며, 전부 설계(§3.7)와 일치하는 동작을 확인했다. 신규로 발견된 결함은 없다.

## 4. 신규 라인 커버리지 (NFR-W3-2, ≥75%)

```
Name                     Stmts   Miss  Cover   Missing
------------------------------------------------------
src\pptx_md\validator.py   206      3    99%   115-117, 213
```

`git blame` 확인 결과 미커버 3줄(115-117, 213)은 커밋 `6c234dc7`(2026-06-28, 기존 FR-14) 소속이며 이슈 #80 신규 코드가 아니다. FR-29 신규 코드(§3.7 4함수 + 경고 상수)는 보강 테스트 포함 시 100% 커버 — NFR-W3-2(≥75%) 충족(초과 달성). 전체 스위트 라인 커버리지 95.20%.

## 5. 품질 게이트 (AC8, PYTHONPATH 강제 하 재실행)

| 게이트 | 명령 | 결과 |
|--------|------|------|
| 린트 | `ruff check .` | All checks passed! |
| 포맷 | `black --check .` | All done! 42 files unchanged |
| 타입 | `mypy src/` | Success: no issues found in 17 source files |
| 테스트 | `pytest -q` | 677 passed, 3 skipped, 0 failed |
| 커버리지 | `pytest --cov=src/pptx_md --cov-report=term-missing` | Total coverage 95.20%(≥75%), `validator.py` 99% |

## 6. 범위 확인 (소유권 격리)

```
$ git diff master...HEAD --stat
docs/30-test/TR-80.md    | 108 ++
src/pptx_md/validator.py | 236 ++++++++++++++++++++++++++++
tests/test_validator.py  | 293 +++++++++++++++++++++++++++++++++
3 files changed, 637 insertions(+)
```

`assembler.py`/`api.py`/`heading.py`/`metadata.py`/`notes.py` 등 타 이슈(FR-28 A/B/C) 파일 무접촉. 구현계획·소유권 격리 준수 확인.

## 7. 발견 결함

**없음.** developer 테스트(18건) + 브랜치 기보강 QA 테스트(8건) + 본 라운드 독립 신규 프로브(10건) 총 36개 검증 경로에서 AC1~AC8 위반 사례를 발견하지 못했다. `_check_pipe_tables`/`_check_duplicate_headings`/`_check_control_chars`/`_check_empty_slides` 모두 ARCH-Wave3 §3.7 알고리즘과 일치하게 구현됨.

## 8. 결론

- 이슈 #80 AC1~AC8 전건 충족. 결함 0건.
- 격리 worktree 검증 중 전역 site-packages editable 포인터가 병렬 에이전트 간 경합으로 다른 워크트리를 가리키는 현상을 발견(과거 TR-77 관찰과 동일 패턴) — `PYTHONPATH` 강제로 우회하여 전 게이트를 신뢰성 있게 재현했다. 이 자체는 이슈 #80 의 결함이 아니라 검증 환경의 공유 자원 경합 특성으로, PM/향후 병렬 디스패치 시 참고 사항으로 남긴다(RISKS 참조).
- **RESULT: DONE**
