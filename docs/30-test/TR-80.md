# TR-80 Validator 신규 4규칙 (표/중복헤딩/제어문자/빈슬라이드) [W-D]

> 대상: PR #83 / 브랜치 `feature/80-fr29-validator-rules` (검증 시점 HEAD `dd08c0b`)
> 요구 정본: `docs/10-requirements/REQ-wave3.md` §3 FR-29 AC1~AC7, NFR-W3-2(신규 라인 커버리지 ≥75%)
> 설계 정본: `docs/20-design/ARCH-Wave3.md` §3.7, ADR-619
> 수용 기준(이슈 #80 본문): AC1~AC8
> 실행: 2026-07-06 / 명령: `pytest -q`, `pytest --cov=src/pptx_md --cov-report=term-missing`, `ruff check . && black --check .`, `mypy src/`
> 검증 환경: 격리 worktree(`.claude/worktrees/agent-a5de300e44c29fba0`, 브랜치 `verify-80`=FETCH_HEAD of `feature/80-fr29-validator-rules`)에서 `pip install -e . --no-deps` 재실행 후 `import pptx_md` 경로가 본 worktree `src/pptx_md`를 가리킴을 확인(다른 worktree 오염 아님).

## 1. 전체 스위트 결과

```
677 passed, 3 skipped, 59 warnings in 154.12s (0:02:34)
Required test coverage of 75% reached. Total coverage: 95.20%
```

- 실패 0건, 회귀 0건(FR-14/기존 스위트 669건 전건 유지 + 신규/보강 8건 = 677건).
- `tests/test_validator.py` 단독: **71 passed** (기존 FR-14 63건 + FR-29 developer 신규 없음(개발자 추가분은 이 63건에 포함) — 상세는 §2 참고).

> 참고: developer 커밋(`dd08c0b`)이 추가한 FR-29 테스트는 기존 FR-14 8클래스(AC1~AC8, 45건) 뒤에 `TestFr29Ac1~Ac7` 7클래스(18건)로 추가되어 `tests/test_validator.py` 63건을 구성. 본 TR 작업에서 QA 독립 보강 클래스 `TestFr29QaSupplementIndependent`(8건)를 동일 파일에 추가하여 63→71건.

## 2. AC → 테스트 케이스 매트릭스 (커버리지 100%)

| TC | 대응 AC | 시나리오 | 출처 | 결과 |
|----|--------|---------|------|------|
| TC-80-01 | AC1 | 헤더+구분행만, 데이터 행 0 → "깨진 표" 경고 1건, valid 불변 | developer | PASS |
| TC-80-02 | AC1 | 코드펜스 내부 `\|` 라인 → 표 오인 없음(경고 0) | developer | PASS |
| TC-80-03 | AC1(대조) | 데이터 있는 정상 표 → 깨진 표 경고 없음 | developer | PASS |
| TC-80-04 | AC1(경계) | 2번째 행이 구분행 아님 → 표 미확정(경고 없음) | developer | PASS |
| TC-80-05 | AC1(QA보강) | 헤더 셀 내 이스케이프 파이프(`\|`)가 열 계산을 깨지 않음 | tester | PASS |
| TC-80-06 | AC2 | 데이터 행 열 수 < 헤더 → 열 수 불일치 경고 1건 | developer | PASS |
| TC-80-07 | AC2(경계) | 데이터 행 열 수 > 헤더 → 열 수 불일치 경고 1건 | developer | PASS |
| TC-80-08 | AC2(대조) | 열 수 일치 → 경고 없음 | developer | PASS |
| TC-80-09 | AC2(QA보강) | 열 불일치 데이터 행이 여러 개여도 표당 경고는 1건(과다 경고 회귀 가드) | tester | PASS |
| TC-80-10 | AC3 | 인접(사이 본문 없음) 동일 텍스트 헤딩 2줄 → 중복 인접 헤딩 경고 1건 | developer | PASS |
| TC-80-11 | AC3(경계) | 사이에 본문 있으면 인접 아님 → 경고 없음 | developer | PASS |
| TC-80-12 | AC3(경계) | 사이에 `---` 슬라이드 구분자 → 인접 아님 → 경고 없음 | developer | PASS |
| TC-80-13 | AC3(대조) | 텍스트 다르면 인접해도 경고 없음 | developer | PASS |
| TC-80-14 | AC3(QA보강) | 동일 텍스트 헤딩 3연속 → 인접 쌍마다 경고 2건(다중 케이스 회귀 가드) | tester | PASS |
| TC-80-15 | AC3(QA보강) | 동일 헤딩 사이 HTML 주석(FR-28 메타) 개입 → 인접 아님(경고 없음) | tester | PASS |
| TC-80-16 | AC4 | `\v` 1개 잔존 → 경고에 "1개" 포함 | developer | PASS |
| TC-80-17 | AC4(경계) | `\v` 3개 잔존 → 경고에 "3개" 포함 | developer | PASS |
| TC-80-18 | AC4(대조) | `\v` 없음 → 경고 없음 | developer | PASS |
| TC-80-19 | AC5 | 모든 셀 공백인 pipe table → 전공백 표 경고 1건 | developer | PASS |
| TC-80-20 | AC5(대조) | 일부 셀 내용 있음 → 전공백 경고 없음 | developer | PASS |
| TC-80-21 | AC6 | 헤딩만 있고 본문 없는 슬라이드 블록(중간) → 빈 슬라이드 경고 1건 | developer | PASS |
| TC-80-22 | AC6(경계) | 헤딩+HTML 주석만 있는 블록도 빈 슬라이드로 판정 | developer | PASS |
| TC-80-23 | AC6(대조) | 모든 블록에 본문 있음 → 경고 없음 | developer | PASS |
| TC-80-24 | AC6(경계) | 연속 `---` 사이 0줄 블록도 빈 슬라이드로 판정 | developer | PASS |
| TC-80-25 | AC6(경계) | frontmatter 앞 공백 줄 있어도 정상 스킵(오탐 없음) | developer | PASS |
| TC-80-26 | AC6(QA보강, 갭 보완) | **문서 맨 앞(첫 `---` 이전) 슬라이드 블록**이 비어도 빈 슬라이드 경고(developer 스위트는 항상 선행 구분자가 있는 블록만 테스트 — 첫 블록 케이스 누락 확인 후 보강) | tester | PASS |
| TC-80-27 | AC6(QA보강, 갭 보완) | 헤딩 뒤 **펜스 코드블록만** 있는 슬라이드는 본문으로 간주되어 빈 슬라이드 경고 없음(빈 판정이 펜스 콘텐츠까지 과잉 검출하지 않는지 확인) | tester | PASS |
| TC-80-28 | AC7 | 결함 없는 정상 문서(표·헤딩·구분자 정상) → 신규 경고 0, valid=True | developer | PASS |
| TC-80-29 | AC7 | 기존 FR-14 헤딩 시작 경고는 유지되고 신규 규칙과 혼입되지 않음(회귀 0) | developer | PASS |
| TC-80-30 | AC7(예외) | 기형적 pipe 콘텐츠(`\|\|\|\|\|`) → 무예외, `ValidationResult` 반환 | developer | PASS |
| TC-80-31 | AC7 | YAML frontmatter 블록은 표/빈 슬라이드 분석에서 제외(오탐 없음) | developer | PASS |
| TC-80-32 | AC7(QA보강) | 기존 미닫힘 펜스(valid=False)와 신규 규칙 공존 시에도 valid 는 기존 규칙에만 좌우됨(신규 규칙이 valid 를 건드리지 않음을 조합 상황에서 재확인) | tester | PASS |
| TC-80-33 | AC7(QA보강, 갭 보완) | frontmatter 여는 구분자만 있고 **닫는 구분자가 없는** 기형 입력 → 무예외(`_strip_frontmatter_lines` fallback 경로, developer 스위트 미커버 라인 238 커버) | tester | PASS |
| TC-80-34 | AC8 | `ruff check . && black --check .` exit 0 | tester(별도 명령) | PASS |
| TC-80-35 | AC8 | `mypy src/` exit 0 | tester(별도 명령) | PASS |
| TC-80-36 | AC8 | `pytest -q` 전체 스위트 실패 0, 회귀 0(677 passed / 3 skipped) | tester(별도 명령) | PASS |

**AC 커버리지: AC1~AC8 전건 대응 TC 존재·통과 (100%).**

## 3. developer 단위 테스트 "형식적 테스트" 점검

developer 테스트(`TestFr29Ac1~Ac7`, 18건)를 검토한 결과 전부 `validate_markdown()`의 **실제 Then**(경고 문자열 substring·건수·`valid` 값)을 단언하며, mock/내부 구현 호출 검증 등 형식적 패턴은 없음. 각 AC 마다 정상(발동) 케이스 + 최소 1개의 대조(미발동) 케이스를 갖춰 실질적 회귀 방지력이 있음을 확인. 결함 없음.

다만 §4 리스크에 기술한 **경계 케이스 2건 누락**(TC-80-26, TC-80-27에 해당하는 "첫 슬라이드 블록"·"펜스 코드만 있는 슬라이드")과 **frontmatter 미닫힘 fallback 라인(238) 미커버**를 발견하여 `tests/test_validator.py`에 `TestFr29QaSupplementIndependent`(8건, TC-80-05/09/14/15/26/27/32/33)로 보강함. 보강 후 전건 PASS, 결함으로 이어지지 않음(설계·구현이 이미 해당 경로를 올바르게 처리하고 있었음 — 테스트 커버리지만 보완).

## 4. 신규 라인 커버리지 (NFR-W3-2, ≥75%)

```
Name                     Stmts   Miss  Cover   Missing
------------------------------------------------------
src\pptx_md\validator.py   206      3    99%   115-117, 213
```

- `validator.py` 파일 전체 99% (206 stmts 중 미커버 3줄).
- `git blame` 확인 결과 미커버 3줄(115-117, 213)은 전부 커밋 `6c234dc7`(2026-06-28, 기존 FR-14) 소속이며 이슈 #80(`dd08c0b`) 신규 코드가 아님. **FR-29 신규 코드(라인 47-52, 54-69, 217-409)는 보강 테스트 포함 시 100% 커버** — NFR-W3-2(≥75%) 충족(초과 달성).
- QA 보강 테스트(TC-80-33)로 기존 미커버였던 라인 238(`_strip_frontmatter_lines`의 닫는 `---` 미존재 fallback)을 신규로 커버시킴(보강 전 98%→99%).

## 5. 무예외·회귀 확인

- `pytest -q` 전체 스위트: **677 passed, 3 skipped, 0 failed** (기존 3 skip 은 FR-29/본 이슈와 무관, 회귀 아님).
- FR-14 기존 8클래스(45건) 전건 PASS — 헤딩 시작/점프/빈 문서/미닫힘 펜스/코드블록 제외 규칙 회귀 0.
- 신규 4규칙 전부 `valid` 불변 확인(TC-80-01/06/10/16/19/21 및 TC-80-32 조합 케이스에서 `valid` 값 명시 단언).
- 예외 케이스(TC-80-30, TC-80-33, 및 developer AC7 스위트의 이진/유니코드/긴 문자열 등) 전부 무예외.

## 6. 발견 결함

**없음.** 코드 리뷰(§3.7 알고리즘 대조) 및 8건의 독립 경계/조합 프로브(문서 맨 앞 빈 블록, 펜스 코드 본문 인정, 이스케이프 파이프, 다중 열 불일치, 3연속 중복 헤딩, HTML 주석 개입, 미닫힘 frontmatter fallback, valid-신규규칙 조합)를 실행했으나 AC 위반 사례를 발견하지 못함. `_check_pipe_tables`/`_check_duplicate_headings`/`_check_control_chars`/`_check_empty_slides` 모두 ARCH-Wave3 §3.7 알고리즘과 정확히 일치하게 구현됨.

## 7. 품질 게이트 (AC8)

| 게이트 | 명령 | 결과 |
|--------|------|------|
| 린트 | `ruff check .` | All checks passed (본 TR 보강 중 발견된 E501 3건 수정 후) |
| 포맷 | `black --check .` | All done, 42 files unchanged |
| 타입 | `mypy src/` | Success: no issues found in 17 source files |
| 테스트 | `pytest -q` | 677 passed, 3 skipped |

## 8. 결론

- AC1~AC8 전건 충족 확인. 결함 0건.
- `tests/test_validator.py`에 QA 독립 보강 테스트 8건(`TestFr29QaSupplementIndependent`) 추가 — developer 스위트가 다루지 않은 경계 케이스(첫 슬라이드 블록·펜스 코드 본문·이스케이프 파이프·다중 불일치·3연속 중복·HTML주석 개입·frontmatter 미닫힘·valid 조합)를 메움.
- **RESULT: PASS**

