## TR-81 골든 회귀 게이트 (off/on 2종 + CI hard-fail)

실행: 2026-07-11 / 대상: PR #87 (`feature/81-fr29-golden-gate`, HEAD `eb7bac0`), 격리 worktree
(`.claude/worktrees/agent-a6cb89aa4c7923340`), `PYTHONPATH=<worktree>/src` 강제(전역 editable
포인터 경합 회피 — import 경로가 이 worktree `src/pptx_md/__init__.py` 임을 사전 확인).

명령:
- `pytest -q` / `pytest --cov=src/pptx_md --cov-report=term-missing`
- `pytest tests/test_golden_regression.py -v`
- `ruff check . && black --check .` / `mypy src/`
- 독립 QA 스크립트: `tc_81_structural.py` (developer 의 `_build_presentation`/`_compare_or_update`/
  `_dod_warnings` 를 재사용하지 않고, 별도로 손수 조립한 `PresentationIR` + `assemble_document` +
  `validate_markdown` 직접 호출로 20건 독립 재검증)
- 실제 골든 파일 변조→복원 사이클(bash + pytest, 2회, `wave3_off.md`/`wave3_on.md` 각 1회) —
  AC2/AC9 hard-fail 과 AC6 env-guard 실효성을 developer 코드 재실행이 아닌 **직접 파일 조작**으로 실증.

### AC 매트릭스 (이슈 #81 AC1~AC7 = FR-29 AC8~AC10)

| TC | 대응 AC | 시나리오 | 결과 |
|----|--------|---------|------|
| TC-81-01 | AC1 정상 | `tests/golden/wave3_off.md`/`wave3_on.md` 2종 실재 확인 | PASS |
| TC-81-02 | AC1 경계 | 골든 산출물 자체에서 섹션표지(`## 표지`)·계층분해(`## A`/`### B`/`#### C`)·`has_diagram: true` 메타·`> notes`·frontmatter(`---\n`) 5요소 전부 실재 확인(코드가 아닌 산출물 텍스트로 직접 확인) | PASS |
| TC-81-03 / 03b | AC1 예외 | `wave3_off.md` 임시 이동(부재 재현) → 부재 확인 → 원복 확인 | PASS |
| TC-81-04 / 04b | AC2 정상 | developer 코드 미재사용, 독립 손수 조립 IR 로 `assemble_document(pres)`(off)/`assemble_document(pres, 4옵션 True)`(on) 재산출 → 두 골든과 바이트 동일 | PASS |
| **TC-81-05** | **AC2/AC9 경계 (핵심 회귀 실증)** | 실 `tests/golden/wave3_off.md` 말미 1줄 변조(`감사합니다.`→`감사합니다!!! (변조됨)`) → `pytest -k test_ac9_off_golden_byte_identical` 실행 | **FAIL 재현 성공** — `AssertionError` + `difflib.unified_diff`(`@@ -16,4 +16,4 @@`, `-...변조됨` / `+감사합니다.`) 출력 확인 |
| TC-81-06 | AC2/AC9 복구 | `git checkout -- tests/golden/wave3_off.md` 원복 → 동일 테스트 재실행 | PASS (1 passed) |
| TC-81-07 / 07b | AC3 정상 | 독립 IR 2개(별도 객체) off/on 각각 렌더 → 바이트 동일 | PASS |
| TC-81-08 | AC3 경계 | on 옵션 kwargs 순서를 뒤바꿔 호출(`include_notes=True, emit_frontmatter=True, emit_toc=True, heading_hierarchy=True`) → 순서 무관 동일 산출 | PASS |
| TC-81-09 | AC3 예외(반복) | 동일 옵션 5회 반복 렌더 → 전부 동일(은닉 비결정성 부재) | PASS |
| TC-81-10 / 10b | AC4 정상 | 실제 골든 파일 텍스트에 `validate_markdown` 직접 호출(테스트 파일 재사용 없음) → DoD 4종(중복/`\v`/전공백표/깨진표) 경고 0 | PASS |
| TC-81-11 | AC4 경계 | on 골든: `빈 슬라이드` 경고는 존재(표지 슬라이드) but DoD 4종 집합에는 미포함 확인 | PASS |
| **TC-81-12a~d** | **AC4 예외 (회귀 실증)** | on 골든 사본에 4개 결함을 각각 인위 주입(중복 헤딩 복제 / `\v\v\v` 추가 / 표 셀 전공백화 / 데이터행 제거) → `validate_markdown` 이 4종 전부 실제 탐지 | **PASS 4/4** — `중복 인접 헤딩`/`제어문자 \v 잔존: 3개.`/`전공백 표`/`깨진 표: 데이터 행이 없습니다.` 각각 확인 |
| TC-81-13 | AC5 정상 | `tests/test_golden_regression.py` 모듈 docstring 에 `N/A`+`이미지`+`커버리지` 명시 확인 | PASS |
| TC-81-14 / 14b | AC5 경계 | 독립 IR DIAGRAM 이미지 `description is None` 확인 + 골든 산출물이 VLM 설명 문단이 아닌 `![슬라이드 2 이미지 1 — diagram]` classification-only 위치 마커로 렌더됨 확인(`assembler.py:763-790` 대조) | PASS |
| TC-81-15 | AC6 정상 | 변조 없는 상태에서 `PPTXMD_UPDATE_GOLDEN` 미설정 전체 스위트 실행 → 비교만 수행, 파일 불변(초기 전체 실행에서 확인) | PASS |
| **TC-81-16** | **AC6 경계 (회귀 실증)** | 실 `wave3_on.md` 1줄 변조(`감사합니다.`→`TAMPERED_ON_GOLDEN_TEXT`) → `PPTXMD_UPDATE_GOLDEN` **미설정** 상태로 `pytest -k test_ac9_on_golden_byte_identical` 실행 | **FAIL 재현** + 실행 후 파일이 여전히 변조 상태로 남음(자동 복구 없음, `grep -c TAMPERED... == 1`, `git diff` 잔존) — 우발 갱신 방지 확인 |
| TC-81-17 | AC6 예외/복구 | 변조 상태 유지한 채 `PPTXMD_UPDATE_GOLDEN=1` 설정 후 동일 테스트 재실행 | PASS(1 passed) — 파일이 올바른 내용으로 **재기록**되어 `git diff tests/golden/wave3_on.md` 가 다시 빈 결과(원본과 완전 일치)로 복원됨 확인. `ci.yml` diff 0 재확인(`git diff master -- .github/workflows/ci.yml` 라인수 0) |
| TC-81-18 | AC7 정상 | `pytest -q`(전체) 및 `pytest --cov=...` | PASS — **765 passed, 3 skipped**, 커버리지 **95.44%**(≥75% NFR-W3-2) |
| TC-81-19 | AC7 경계 | `ruff check .` / `black --check .` | PASS — "All checks passed!" / "49 files would be left unchanged" |
| TC-81-20 | AC7 예외 | `mypy src/` | PASS — "Success: no issues found in 20 source files" |

**AC 커버리지: 7/7 (100%)** — AC1~AC7 전건 정상·경계·예외 최소 1건씩 확보(AC5 는 관측 가능한 "예외" 조건이 사실상 없어 경계 2건으로 대체, 별도 결함 아님).

### 핵심 회귀 실증 (요청 사항 직접 재현)

1. **골든 변조 → 실패**: `wave3_off.md` 말미 1줄을 실제로 바꾼 뒤 `pytest`를 재실행하면 `AssertionError`가
   `difflib.unified_diff` 본문(`@@ -16,4 +16,4 @@`, 변경 전/후 라인)과 함께 즉시 발생하고 해당 테스트가
   **FAIL** 로 전환됨을 확인했다(하드페일 실효성, D-6/AC9). 원복 후 재실행 시 **PASS** 로 정확히 복귀했다.
2. **env-guard 미설정 시 재기록 안 됨**: `wave3_on.md`를 변조한 채로 `PPTXMD_UPDATE_GOLDEN` **미설정** 상태로
   실행하면 테스트는 FAIL 하고 파일은 변조된 그대로 남는다(자동 침묵 갱신 없음, 우발 갱신 방지 확인).
   같은 파일을 변조된 채 `PPTXMD_UPDATE_GOLDEN=1` 로 재실행하면 그제서야 올바른 내용으로 재기록되어
   `git diff` 가 0 으로 돌아온다(정확한 원본 재산출 확인). `ci.yml` 은 이 절차 전후로 무변경.
3. 검증 종료 후 `git status`로 `tests/golden/` 이하 완전히 클린(diff 0) 상태를 최종 확인했다 — 조작한
   두 파일 모두 정상 복원됨.

### developer 단위 테스트 형식성 점검

`tests/test_golden_regression.py` 16건을 정독한 결과, 단언이 실제 AC 의 Then 을 검증한다(형식적 테스트
아님): 바이트 비교는 진짜 파일 바이트 대 재산출 바이트, DoD 경고 검사는 실제 `validator.py` 메시지
substring 대조(하드코딩된 `assert True` 류 없음), env-guard 테스트는 `tmp_path` 실제 파일 write/read 로
검증. 광범위 mock 없음. 단, `**_OFF_OPTIONS`/`**_ON_OPTIONS` 언패킹 호출부(L321,326,331,332,337,338,352,
358,365)에 대해 `mypy tests/test_golden_regression.py` 를 독립 실행하면 **18건 `arg-type` 오류**가 실제로
발생함을 재현·확인했다 — developer/reviewer 코멘트의 주장이 정확함을 검증했다. 다만 `pyproject.toml`
`[tool.mypy] files = ["src"]` + 프로파일 §4 `mypy src/` 로 게이트 스코프가 명시적으로 `src/` 한정이므로
**AC7 미충족 아님**(범위 밖). RISKS 에 부채로 기록.

### 커버리지

`pytest --cov=src/pptx_md --cov-report=term-missing` 전체 스위트 TOTAL 1381 stmts / 95.44% (≥75%,
NFR-W3-2 충족). `tests/test_golden_regression.py` 단독 실행 시 커버리지는 45.55%(다른 소스 미가동)로 낮게
나오는 것이 정상 — 전체 스위트 기준으로 게이트 판정.

### 실행 로그 핵심부 (증거)

```
$ PYTHONPATH=<wt>/src python -c "import pptx_md; print(pptx_md.__file__)"
<wt>\src\pptx_md\__init__.py                      # 격리 확인

$ PYTHONPATH=<wt>/src pytest -q
765 passed, 3 skipped, 59 warnings in 126.15s
Required test coverage of 75% reached. Total coverage: 95.44%

$ PYTHONPATH=<wt>/src pytest tests/test_golden_regression.py -v
16 passed in 3.00s (fixture-only run coverage 45.55% — informational only)

$ ruff check .
All checks passed!

$ black --check .
All done! 49 files would be left unchanged.

$ mypy src/
Success: no issues found in 20 source files

$ mypy tests/test_golden_regression.py
Found 18 errors in 1 file (arg-type, **_OFF_OPTIONS/**_ON_OPTIONS 언패킹) — 스코프 밖(files=["src"]), AC7 무관 참고

# 변조 재현 (AC2/AC9)
$ python -c "... tests/golden/wave3_off.md 말미 1줄 변조 ..."
$ PYTHONPATH=<wt>/src pytest tests/test_golden_regression.py -k test_ac9_off_golden_byte_identical -v
FAILED ... AssertionError: 골든 회귀 실패(AC9): ... hard-fail ...
--- wave3_off.md
+++ <rendered>
@@ -16,4 +16,4 @@
 ## 마무리
-감사합니다!!! (변조됨)
+감사합니다.
1 failed, 15 deselected

$ git checkout -- tests/golden/wave3_off.md
$ PYTHONPATH=<wt>/src pytest tests/test_golden_regression.py -k test_ac9_off_golden_byte_identical -v
1 passed, 15 deselected

# env-guard 재현 (AC6)
$ python -c "... tests/golden/wave3_on.md 말미 1줄 변조 ..."
$ unset PPTXMD_UPDATE_GOLDEN; PYTHONPATH=<wt>/src pytest ... -k test_ac9_on_golden_byte_identical -v
FAILED ... AssertionError ...
$ grep -c TAMPERED_ON_GOLDEN_TEXT tests/golden/wave3_on.md  ->  1   (변조 유지, 미복구)

$ PPTXMD_UPDATE_GOLDEN=1 PYTHONPATH=<wt>/src pytest ... -k test_ac9_on_golden_byte_identical -v
1 passed, 15 deselected
$ git diff --stat tests/golden/wave3_on.md   ->  (빈 결과, 원본과 완전 일치로 재기록 확인)

$ git status --short
 M .claude/settings.local.json     # (worktree 세팅, PR 스코프 무관, 골든/소스 무변경)

$ python <독립 QA 스크립트: tc_81_structural.py>
PASS=20 FAIL=0 TOTAL=20
```

### 결함
없음. 전건 PASS. (참고 사항 — REWORK 아님: `mypy tests/` 스코프 외 18건, RISKS 참조)

### 범위/소유권 재확인
`git diff master --name-status`: `tests/golden/.gitattributes`(A), `tests/golden/wave3_off.md`(A),
`tests/golden/wave3_on.md`(A), `tests/test_golden_regression.py`(A) 4파일만. `src/`·`.github/workflows/ci.yml`
무접촉 확인(diff 0). 검증 중 실 골든 2개 파일을 의도적으로 변조했으나 **모두 검증 종료 전 원복**하여
최종 작업 트리는 클린(TR 문서 추가만 신규 커밋 대상).

### RESULT: DONE
